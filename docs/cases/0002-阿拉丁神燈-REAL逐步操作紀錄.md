# 案例 0002 阿拉丁神燈：REAL 逐步操作紀錄

建立時間：2026-08-18 10:42 Asia/Taipei

本文件只記實際操作時間線；架構與產品規則見 `0002-阿拉丁神燈-REAL完整閉環手冊.md`。

## 2026-08-18

### 10:27 — 使用者校正問題處理原則

指令：有問題問 OpenWorker 和 go-tool。

執行規則固定為：

```text
不知道 current state / host / dependency / acceptance → 問 OpenWorker
不知道 capability / queue / Action / inputs / readiness / status / cancel → 問 go-tool
兩者都沒有答案 → 才列系統缺口
```

### 10:28 — 問 OpenWorker：如何取得 durable current state

檢查 OpenWorker `scripts/case_worklist_action.py`。

答案：存在正式 `show --workspace-root <workspace>`，直接呼叫 `CaseWorklistRuntime.load()` 並輸出 durable Worklist JSON。

結論：Git `case-worklists/0002.json` 只能作模板；真實 current step / assigned_host 必須由本機 durable Worklist回答。

### 10:30 — 問 OpenWorker：舊 0002 run 能否作 current harness rerun

讀取歷史案例紀錄：

- `31923007861`：舊 Shot-1 REAL provenance closure；
- `31925346602` / job `95112376375`：舊工作目錄 P0 REAL PASS；
- `31928045624`：generic runner 被 UL7 接單後 COMPUTERNAME gate skip。

答案：以上均不是新版 `0002-025` PPT governed harness，不得冒充 current run 或直接拿來作 025 rerun。

### 10:32 — 發現 OpenWorker-first 順序缺口

檢查 `go-tool-runtime/cmd/e2e-0002-dispatch/main.go`。

發現：程式在讀 OpenWorker 之前先硬編碼 `COMPUTERNAME == DESKTOP-ODAQN0D`，之後才讀 durable Worklist 的 `assigned_host`。

判定：違反「先問 OpenWorker」。owning repo=`go-tool-runtime`。

### 10:33 — 修 go-tool-runtime harness

commit：`7f901fc0faba0b0ff229b6a898143f6737b7023a`

修後順序：

```text
load D:\AI-Example\0002\.openworker\case-worklist.json
→ 00-openworker-current.json
→ case_id == 0002
→ assigned_host nonempty
→ COMPUTERNAME == assigned_host
→ canonical next step
→ go-tool runtime
→ /agent/query
```

狀態：`IMPLEMENTED / NOT LIVE VERIFIED`。

### 10:34 — 檢查修復 commit 是否觸發 Operator

`7f901fc0...` 修改 `cmd/e2e-0002-dispatch/**`，符合現行 Operator workflow push path。

當時尚未看到 route/dispatch evidence，因此不猜觸發狀態。

### 10:37 — 問 go-tool：能否回答 current Operator queue

檢查 `internal/execution/queue_admin.go` 與 `cmd/gtr-actions-queue`。

go-tool 的答案邊界：queue list/drain 必須經已註冊 capability；workflow-scoped query 使用 capability 綁定的 repository/workflow；不允許 caller 任意傳 repo/workflow；可查 queued/in_progress/waiting/pending；可 preserve/cancel/verify。

發現：當前 Operator 沒有 admin capability，因此 go-tool 無法合法回答 queue 狀態。

### 10:38 — 補 0002 Operator admin capability

新增 `openworker.case0002.operator.admin`，commit：`f998bffa72fdd9b68431f8223f137782f6af3d1a`。

### 10:39 — 補永久 regression test

commit：`1971fe9d6450630feea43388551032e23106e52c`。

### 10:40 — 讓 go-tool 自己做一次只讀 queue query

新增 `.github/workflows/query-case0002-operator-admin.yml`，commit：`d2bcd80a072e0ac842d9f6ec6ac0eb22434396eb`。

### 10:41 — queue evidence 尚未出現

`evidence/ops/latest-case0002-operator-queue.json` 尚未出現；不猜原因。

### 10:43 — 找到真正 current governed harness run

搜尋 go-tool-runtime commit history 時，發現機器 evidence commit：`51a2b3fd9cf6cadc2a8af2fccf52d808f1c4882b`。

該 evidence 給出 harness run `32091092500`。其中 slot 2 在 UL7，route_run=false。

### 10:44 — 查 run `32091092500` jobs

確認 e2e(3) job `95573238814` 在 `DESKTOP-ODAQN0D-R008 / DESKTOP-ODAQN0D`。

### 10:45 — ODA 原始失敗根因

第一個 `actions/checkout@v4` 就因：

```text
EBUSY: resource busy or locked
rmdir C:\github-runners\go-tool-runtime\_work\go-tool-runtime\go-tool-runtime\gtr-bootstrap-31998928453
```

失敗。尚未進 OpenWorker/go-tool。

### 10:46 — 正式 rerun ODA job

rerun `95573238814`，新 job `95575621633`。

### 10:48 — rerun 終態

再次相同 EBUSY，同一鎖目錄，證明不是偶發。

### 10:49 — 問 go-tool 是否有 runner workspace cleanup 能力

沒有找到安全正式 capability。依規則形成 owning gap：self-hosted runner checkout workspace hygiene。

### 10:50 — 安全 owning fix：隔離 checkout

新增 `.github/workflows/operator-e2e-0002-governed-v2.yml`，commit `7e0e31e894d747a2ec48704c526462f4481777e8`。

每個 run/attempt/slot 使用唯一 checkout，`clean=false`，不碰舊鎖目錄、不強殺未知進程。

### 10:52 — v2 REAL pointer 出現

機器 evidence commit：`3cc29336dd1ef17cc8b7c27fd1dd4c2d6c574c3d`。

內容：

```text
harness_run_id = 32092404989
attempt = 1
slot = 2
runner = DESKTOP-ODAQN0D-R008
computer = DESKTOP-ODAQN0D
outcome = failure
source_sha = 7e0e31e894d747a2ec48704c526462f4481777e8
```

這證明 isolated checkout 已跨過舊 EBUSY 並由 ODA 真實接單。

### 10:53 — v2 run `32092404989` jobs

ODA winner job：`95577062854`。

關鍵 steps：

```text
Checkout go-tool isolated = PASS
Route ODA = PASS
Checkout OpenWorker isolated = PASS
Run governed current-step closure = FAILURE
Publish pointer = PASS
```

因此 failure 已進入 governed harness，不再是 checkout 控制面。

### 10:54 — OpenWorker durable current 真實答案

ODA log 已出現：

```text
MILESTONE 00-openworker-current.json
MILESTONE 00a-runtime-health.json
```

後續 block step 顯示 canonical current step 為：

```text
0002-010
```

因此之前「預期已到 025」只是一個推測；**本機 durable OpenWorker authority 的真實 current 是 0002-010**。

這是本案例目前最重要的狀態修正：不得跳過 010/020 直接跑 025。

### 10:55 — 問 go-tool `/agent/query` 時 runtime 被強制關閉

log：

```text
ERROR: Post http://127.0.0.1:8848/agent/query
connection forcibly closed
```

緊接著 go-tool runtime 自己給出真正原因：

```text
Failed to load config:
capability fragment openworker-case0002-operator-admin.yaml:
capability "openworker.case0002.operator.admin"
has unsupported execution mode "queue_admin"
```

所以不是 HTTP/network 問題，而是 capability schema 阻止 runtime 啟動。

### 10:56 — 問 go-tool validator：正式 execution mode 是什麼

`validateCapabilityContract` 的權威答案：

```text
github_action
local_action
cli
```

原 schema 沒有 `queue_admin`。

而 go-tool 已有專門 queue admin service，所以把 admin capability 偽裝成 local_action 不正確。owning gap 應是 schema 尚未正式表達 queue administration。

### 10:57 — 補正式 `queue_admin` execution mode

更新 `internal/config/capability_fragments.go`，新增 `queue_admin` 為受支援 execution mode。

commit：`20f26d009e08f9cb3fa0d383ffba60497466d034`。

既有 `openworker.case0002.operator.admin` regression test 會直接經 `validateCapabilityContract` 驗證此 mode。

### 10:58 — governed v2 觸發範圍補齊

為避免 capability/schema 修復後案例不會自動重跑，v2 workflow push paths 新增：

```text
internal/config/**
capabilities.d/**
```

commit：`8139f349a867a1244e34dc77853f23257e07944a`。

該 commit 本身會觸發下一輪 v2 REAL run。

### 10:59 — 當前真實狀態

目前 durable current 是：

```text
0002-010 Generate fresh structured Director preproduction
```

不是 025。

下一轮必须遵守：

```text
OpenWorker 010
→ go-tool /agent/query
→ comfyx-studio.director.preproduction
→ 010 acceptance
→ OpenWorker 020
→ storyboard.plan
→ 020 acceptance
→ OpenWorker 025
→ presentation.openmaic text-only PPTX
→ 027 user approval
```

当前等待 `8139f349...` 触发的新 v2 machine pointer。不得越级生成 PPT、图片或视频。
