# 案例 0004：DWG → 3D REAL 固定 O87 執行詳實紀錄

> 案例主責：`liuxb99/openworker`
>
> OS 治理：`liuxb99/AI-Engineering-OS`
>
> 工具資訊控制面：`liuxb99/go-tool-runtime`
>
> CAD owning repo：`liuxb99/DWG_todo`
>
> 固定 REAL 主機：`DESKTOP-O87PJNR`（O87）
>
> Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
>
> 建立時間：2026-08-17 14:04（Asia/Taipei）
>
> 狀態：`IMPLEMENTING / FIXED-O87 ROUTING ACTIVE / SOURCE LOCATOR QUEUED`

---

## 0. 文件定位

本文件依案例 0002、0003 的正式案例文檔方式記錄案例 0004 的實際執行，不是單純進度摘要。

從本批開始，所有 consequential REAL step 都必須保留：

- 目的與前置狀態；
- OpenWorker 判斷與工具選擇理由；
- capability / owning repo / workflow / ref；
- normalized inputs；
- run ID / job ID / runner / COMPUTERNAME；
- physical path / size / SHA256 / format；
- reopen / parse / visual / geometry QC；
- PASS / FAIL / BLOCKED；
- 真實 root cause；
- owning repo；
- repair commit；
- rerun evidence；
- next action。

歷史錯誤與被撤回方案保留為 audit trail，不覆寫成「沒發生」。但只有 current architecture 與 current evidence 可決定目前狀態。

---

## 1. REAL Source Authority

### Primary source

```text
original_name = S1-1140926(1).dwg
role = PRIMARY REAL SOURCE
expected_size = 1,385,583 bytes
expected_sha256 = aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6
expected_header = AC1032
canonical_name = source.dwg
canonical_target = D:\AI-Work\jobs\0004-DWG-TO-3D\input\source.dwg
```

目前 source locator request authority：

`source-locator-requests/request.json`

關鍵 normalized inputs：

```text
case_id = 0004
assigned_host = DESKTOP-O87PJNR
workspace_root = D:\AI-Work\jobs\0004-DWG-TO-3D
original_name = S1-1140926(1).dwg
canonical_name = source.dwg
expected_size = 1385583
expected_sha256 = aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6
expected_header = AC1032
```

Search roots 保持 bounded：

```text
D:\AI-Work
D:\AI-Example
D:\AI-Civil
D:\AIWork
```

Locator 必須同時符合 size + SHA256 + header；多個 exact identity match 時不得猜測，必須 fail closed。

---

## 2. 固定機器架構修正紀錄

### 2.1 早期探索：assigned-host shared queue

2026-08-17 曾沿 `assigned_host → shared queue → persistent agent` 路線檢查 O87 production routing，並發現 consumer / provider deployment 邊界不完整。

該探索後來被最新版固定機器文檔取代，不再是案例 0004 正式 product path。

重要結論：

```text
shared queue/provider = RETIRED FOR THIS FIXED-MACHINE CASE
per-host queue = NOT CURRENT PRODUCT ROUTING
Tailscale ingress = NOT CURRENT PRODUCT ROUTING
```

此歷史保留的目的，是避免後續 Agent 再誤判為 current blocker。

### 2.2 最新正式 fixed-machine contract

目前正式方式與案例 0003 已驗證方式一致：

```yaml
runs-on: [self-hosted, Windows, X64, O87]
```

job 開始後再做 machine identity fail-closed：

```text
COMPUTERNAME == DESKTOP-O87PJNR
```

因此：

- runner label 負責 GitHub Actions 調度到固定機器族；
- `COMPUTERNAME` 負責執行前二次身份驗證；
- wrong machine 不允許執行產品步驟；
- 不再增加 queue/agent/provider layer。

固定 O87 source locator 修復提交：

```text
7e1150d4f80c2c73f2110cf2d4bbd8c3501aab14
fix(0004): pin source locator to O87 runner label
```

source locator dispatch commit：

```text
a5795db9fdd57f6a7bf885e44ee44cd2a43b4d3b
ops(0004): dispatch fixed O87 source locator
```

---

## 3. Step C-002 — 固定 O87 source locator dispatch

### 時間

2026-08-17 13:52 起，14:04 再次核對。

### 目的

在 O87 本機 bounded search roots 內找出與 primary source identity 完全一致的 DWG，作為後續 canonical ingress 的唯一來源。

### Workflow

```text
repo = liuxb99/openworker
workflow = .github/workflows/engineering-source-locator-win11.yml
runs-on = [self-hosted, Windows, X64, O87]
identity gate = COMPUTERNAME == DESKTOP-O87PJNR
```

Workflow 第一個產品前 gate：

```text
if COMPUTERNAME != DESKTOP-O87PJNR → throw / fail closed
```

### Dispatch

```text
run_id = 31999411286
job_id = 95296814580
head_sha = a5795db9fdd57f6a7bf885e44ee44cd2a43b4d3b
workflow_run_number = 5
status = queued
conclusion = null
```

截至 2026-08-17 14:04：

```text
job status = queued
runner = 尚未分配
COMPUTERNAME = 尚未執行，因此未知
product output = 尚未產生
```

### 判讀

這個狀態只能記為 `QUEUED`，不能記 PASS、RUNNING 或 source found。

目前沒有 evidence 顯示：

- locator 已在 O87 開始；
- source 已找到；
- source 已 canonical materialize；
- OS Project/Job 已建立。

因此 current product blocker 是：

```text
O87-labelled self-hosted runner 尚未接到 locator job
```

這是 routing availability blocker，不是 DWG geometry/product failure。

---

## 4. Step C-003 — Local Source Ingress Contract 修復

在 locator/ingress 並行開發中，OpenWorker local source ingress entrypoint 已修正為 canonical API。

修復 commit：

```text
77faf411759ecd1f14467c3f89add48fb342d3dd
fix(ingress): align local source entrypoint with canonical API
```

修復內容包括：

- isolated AI-Engineering-OS process lifecycle；
- assigned_host 明確傳入 EngineeringSourceIngress；
- expected SHA256 / size / header 一併驗證；
- source run / repository / producer commit provenance；
- `openworker.local-source-ingress-result.v2`；
- workspace evidence 寫入；
- `LOCAL_SOURCE_INGRESS_REAL_PASS` terminal receipt；
- process terminate/kill cleanup。

這代表「找得到 exact local source 之後，如何 canonical ingress」的程式契約已存在。

但本案例目前仍未取得 O87 REAL pass receipt，所以本步狀態：

```text
IMPLEMENTED / WAITING FOR O87 REAL EXECUTION
```

相關 local ingress workflow：

```text
.github/workflows/engineering-local-source-ingress-win11.yml
```

最新 observed run：

```text
run_id = 31999522689
head_sha = 77faf411759ecd1f14467c3f89add48fb342d3dd
status = pending
```

此 workflow 目前不是案例成功 evidence，因為尚未有 O87 REAL terminal result。

---

## 5. O87 Local Authority 狀態

OpenWorker 已有 O87 local authority snapshot workflow：

```text
.github/workflows/bootstrap-o87-local-authority.yml
runs-on = [self-hosted, Windows, X64, O87]
COMPUTERNAME gate = DESKTOP-O87PJNR
```

它會把 tracked OpenWorker source snapshot 到：

```text
%ProgramData%\go-tool-runtime\work-agent\authorities\openworker-<commit12>
```

並寫：

```text
openworker-current.json
```

最新 observed run：

```text
run_id = 31999522722
head_sha = 77faf411759ecd1f14467c3f89add48fb342d3dd
status = queued
```

這再次支持目前 blocker 在 `O87 label runner availability/registration`，而不是 source locator Python 本身。

---

## 6. Current Stage Snapshot

截至 2026-08-17 14:04 +08:00：

```text
current_stage = STEP C / LOCAL PRIMARY DWG DISCOVERY
current_status = IMPLEMENTING
assigned_host = DESKTOP-O87PJNR
workspace = D:\AI-Work\jobs\0004-DWG-TO-3D
primary_source_identity = VERIFIED BEFORE O87 MATERIALIZATION
fixed_machine_routing = IMPLEMENTED
source_locator = QUEUED
local_ingress_contract = IMPLEMENTED / NOT YET REAL VERIFIED FOR CASE 0004
OS ProjectID = not yet accepted
OS JobID = not yet accepted
JobBinding = not yet accepted
cad.open_dwg = not yet executed for user REAL source
```

### Current blocker

```text
O87 fixed-label runner 尚未接到 queued workflow
```

### Owning layer

```text
GitHub self-hosted runner registration/availability for O87
```

不是：

```text
DWG_todo parser
OpenCADStudio
Blender
AI-Engineering-OS Artifact Registry
```

因為這些產品階段還沒開始。

---

## 7. 下一個正式動作

只處理 current blocker，不重開架構設計。

```text
1. 核對最新版 O87 runner-label bootstrap/registration 方法。
2. 若 O87 label 尚未掛上，沿案例 0003/UL7 同一做法補 O87 label。
3. 讓 run 31999411286 或其同契約 rerun 被 O87 接單。
4. 記錄 runner name + COMPUTERNAME。
5. Locator exact source identity PASS。
6. 執行 local source ingress。
7. 取得 ProjectID / JobID / ArtifactID / JobBinding / canonical source path。
8. 在 O87 canonical source 上重算 size / SHA256 / header。
9. 只有 identity 全一致後才進 `cad.open_dwg`。
```

---

## 8. Case 0004 後續產品鏈（不得跳步）

```text
Exact local source locator
→ canonical OpenWorker/OS ingress
→ Project / Job / JobBinding
→ cad.open_dwg REAL
→ Model Space extents / overview render
→ repeated visual search / story regions
→ query_bounds / real handles
→ Story Design JSON + PNG
→ OpenWorker visual review decision
→ approved materialization
→ anchors / story registration / residual validation
→ Building World Coordinates
→ production building.glb
→ Blender 5.2 REAL reopen/render
→ native AC1032 ACIS Solid3D DWG
→ DwgReader/OpenCAD reopen
→ 3DSOLID/layer validation
→ Artifact Registry
→ current revision Review
→ Delivery Revision
→ Drive Review Bundle
→ ChatGPT semantic/visual QC
→ final delivery/website
```

每一步完成後，都要在案例文檔追加同等粒度的 run/job/runner/output/hash/QC/repair/rerun 紀錄。

---

## 9. REAL CLOSED 判定

目前案例 **不得**標 `REAL CLOSED`。

至少仍缺：

```text
[ ] O87 source locator REAL PASS
[ ] canonical source.dwg materialization
[ ] OS Project/Job/JobBinding
[ ] O87 source hash/header revalidation
[ ] cad.open_dwg REAL
[ ] raw overview/render
[ ] Story Region / structural recognition
[ ] Story Design review/materialize
[ ] story registration
[ ] Building World Coordinates
[ ] building.glb
[ ] Blender REAL reopen/render
[ ] native ACIS Solid3D DWG
[ ] native DWG reopen/QC
[ ] OS Artifact Registry
[ ] Review approval
[ ] Delivery Revision
[ ] Drive/ChatGPT final quality review
[ ] final package / website
```

只有全部有 REAL evidence 才能改成：

```text
CLOSED / REAL VERIFIED / MANUAL REPRODUCIBLE
```
