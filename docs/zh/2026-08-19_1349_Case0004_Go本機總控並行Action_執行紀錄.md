# Case 0004：Go 本機總控＋並行 Action 詳細執行紀錄

建立時間：2026-08-19 13:49 +08:00  
最後更新：2026-08-19 14:xx +08:00  
Case：`0004`  
固定主機：`DESKTOP-O87PJNR`（O87）  
固定 Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`  
OpenWorker：`127.0.0.1:8787`  
go-tool 本機總控／durable queue：`127.0.0.1:8848`  
Worklist revision：`142`  

## 0. 本輪硬規則

1. 案例 business execution 的唯一正常路線為：

```text
ChatGPT / local model
→ openworkerctl / Go Case Engine
→ go-tool-runtime :8848 local supervisor
→ durable local-work queue
→ 4-slot local executors
→ OpenWorker :8787 process kernel / coordinator
→ local DWG_todo / AI-OpenSees / Blender / Engineering-OS capability
```

2. GitHub Actions **不得**作 Case 0004 business scheduler、status transport 或成果 transport。
3. GitHub Actions 僅允許做：程式碼更新、測試、安裝／升級、修復診斷。
4. 固定機器 authority 是 Windows `COMPUTERNAME == DESKTOP-O87PJNR`。
5. 本輪不得重跑已經有實機 evidence 的 0004-010～0004-040；先 reconcile，再從 canonical next step `0004-045` 接續。
6. 每一步都要留下：dispatch、claim、work_id、agent slot、開始／完成時間、實體路徑、SHA256、驗收結果、blocker、repair。
7. 視覺／樓層辨識採 fail-closed；沒有實體證據的樓層保留 unresolved，不得硬猜。

## 1. 本輪開始前的真實 checkpoint

舊 Case 0004 實機狀態 revision 141 已確認：

- canonical next step：`0004-045`
- `0004-010`：PASSED
- `0004-020`：PASSED
- `0004-030`：PASSED
- `0004-040`：PASSED
- `0004-045`：READY
- `0004-047` 之後：尚未完成

因此本輪不從頭重做，而是把已完成 evidence 搬入新版 Go Worklist revision 142。

## 2. 新版 Go Controller 缺口發現與修復

### 2.1 缺口 GOC4-001：Go Case Controller 只接受 0005

發現：`go-runtime/internal/casecontroller/controller.go` 的 Bootstrap 原本硬編碼只允許 `0005`。

影響：即使 Case 0004 已有 Worklist，也無法用新版 Go Case Engine bootstrap。

修復：

- 新增 `supportedCaseID()`。
- 正式允許 `0004` 與 `0005`。
- `readySteps()` 同時接受 `PENDING` 與既有 checkpoint 的 `READY`。

提交：`af823a4c98fc73fe2acb1322084964c75634f44b`

驗證：新增 Case0004 bootstrap 永久測試；revision 142 的 `0004-045 READY` 必須成為唯一 ready step。

### 2.2 缺口 GOC4-002：Go Continue 只接受 0005

發現：`go-runtime/internal/casecontroller/continue.go` 原本 hard-code `0005`，而且沒有 Case 0004 step mapping。

修復：

- Continue 改用 `supportedCaseID()`。
- 新增 `0004-045` mapping。
- Worklist action `cad.build_story_index` 不直接當 transport；正式映射為：

```text
capability_id = dwg.story_index.execute.case-worklist
method        = cad.build_story_index
case_step     = 0004-045
assigned_host = DESKTOP-O87PJNR
workspace     = D:\AI-Work\jobs\0004-DWG-TO-3D
```

- durable work 仍送到 `POST 127.0.0.1:8848/api/execution/local-work`。
- `work_id` 使用 Case/step/capability/revision deterministic hash，確保 idempotent。

提交：`e682dc00c084cab4a2a1da690c233dbd20785984`

永久測試：`continue_0004_test.go`，提交 `102817594fd0a3dfd6b52d3108f166c11305b9e2`。

### 2.3 缺口 GOC4-003：openworkerctl 只認 0005 / ODA

發現：`go-runtime/internal/controlcli/controlcli.go` 的 `caseConfig()` 原本只接受 Case 0005，並固定 ODA。

修復：新增 Case 0004：

```text
case_id       = 0004
machine       = DESKTOP-O87PJNR
workspace     = D:\AI-Work\jobs\0004-DWG-TO-3D
manifest      = case-worklists\0004.json
spec          = case-specs\0004.json
server        = http://127.0.0.1:8848
workers       = 4
```

提交：`1f4ae291bdcfe45219ee574f547c0ac62860955a`

永久測試：新增 `TestCaseConfigCase0004`，提交 `03a4b3c1b1ec6953bd99e7b46925808d6e6ef6cf`。

### 2.4 缺口 GOC4-004：沒有 Case 0004 Go spec

修復：新增 `case-specs/0004.json`。

其中明確規定：

- canonical controller：`openworker-go-case-engine/v1`
- Python controller：禁止
- queue authority：`go-tool-runtime:8848`
- execution route：`local_supervisor`
- OpenWorker process kernel：`:8787`
- 必須 `REAL_VERIFIED`
- 必須 `OPERATIONAL`
- 必須 fresh 4 claim slots + 4 executor slots
- GitHub business execution：禁止
- GitHub fallback：禁止
- review artifact transport：Google Drive API
- fixed machine：O87

提交：`822e66f813819e2ad2ff9a2be3e4348bf827752e`

### 2.5 缺口 GOC4-005：Case 0004 Worklist 還是舊 controller metadata

修復：Worklist 升級 revision `142`。

提交：`8ff72006ddc0ba5e38c55ca5205227675610be34`

重要變更：

- authority → `go-tool-runtime-local-supervisor`
- canonical controller → `openworker-go-case-engine/v1`
- queue → `:8848`
- 4-slot claim/executor verification
- queue-owned fanout
- OpenWorker 僅 coordinator，不建立 business child jobs
- GitHub Action business/fallback 關閉
- 010～040 的既有真實 evidence 正式 reconcile 進 revision 142
- 045 為唯一 READY

### 2.6 缺口 GOC4-006：O87 部署 workflow 沒有先跑 Go 全測試

修復：`bootstrap-openworker-node-o87.yml` 安裝前新增：

```text
go test ./...
```

只有測試通過才 build/install OpenWorkerNode。

提交：`ad13a54164898e38ad63c6b504eb9a836c44c6bc`

本文件提交帶 `[bootstrap-o87-node]`，用途僅為讓 O87 安裝最新版程式；**不代表 Case business step 已執行**。

## 3. Step-by-step Case execution ledger

以下為 Case 0004 正式逐步紀錄。每個 step 都區分「既有實機 evidence」、「本輪 reconcile」、「本輪實際 dispatch」。沒有實際 ACK／artifact 就不得寫成完成。

### 0004-010 — Locate exact primary DWG on O87

狀態：`PASSED / RECONCILED`

目的：在固定 O87 找到唯一 primary DWG，建立來源 SHA256 authority。

既有實機 evidence：

- run_id：`32008732425`
- source_path：`D:\AI-Work\jobs\0004-DWG-TO-3D\input\source.dwg`
- SHA256：`aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6`

本輪處理：不重跑；原 evidence 寫入 revision 142。

驗收：PASS。

### 0004-020 — Canonical source ingress + OS Project/Job + JobBinding

狀態：`PASSED / RECONCILED`

既有實機 evidence：

- run_id：`32009612975`
- project_id：`prj_3e0dda459b582aed83e0367c755660e5`
- job_id：`job_f85f3e01cbe1f998c16ac2b772db4fc1`
- canonical_path：`D:\AI-Work\jobs\0004-DWG-TO-3D\input\source.dwg`
- SHA256：同 0004-010
- job_binding：`D:\AI-Work\jobs\0004-DWG-TO-3D\.openworker\job-binding.json`
- reconciliation repair：`R-0004-020-003`
- evidence：`D:\AI-Work\jobs\0004-DWG-TO-3D\evidence\source-ingress\local-run-32009612975.json`

本輪處理：不重跑；正式 reconcile 進 revision 142。

驗收：PASS。

### 0004-030 — Open primary DWG

狀態：`PASSED / RECONCILED`

既有實機 evidence：

- run_id：`32011733568`
- job_id：`job_f85f3e01cbe1f998c16ac2b772db4fc1`
- receipt：`D:\AI-Work\jobs\0004-DWG-TO-3D\evidence\dwg-cad-local\case0004-open-32011733568\receipt.json`
- source SHA256：`aaadbd84...f3192f5b6`

本輪處理：不重跑。

驗收：PASS。

### 0004-040 — Model extents + REAL overview render

狀態：`PASSED / RECONCILED`

既有實機 evidence：

- extent receipt：`D:\AI-Work\jobs\0004-DWG-TO-3D\evidence\dwg-cad-local\case0004-extents-32011900313\receipt.json`
- overview：`D:\AI-Work\jobs\0004-DWG-TO-3D\dwg\exports\default\visual-search\case0004-overview.png`
- overview SHA256：`5cee03340cbbcad51e412b46b85bda9dcaac22b193586b953bbfd5134039103e`

本輪處理：不重跑。

驗收：PASS。

### 0004-045 — Build persistent Story Index

狀態：`READY / WAITING FOR NEW GO LOCAL DISPATCH`

正式 execution route：

```text
openworkerctl case continue 0004
→ Go Case Engine
→ POST :8848/api/execution/local-work
→ capability dwg.story_index.execute.case-worklist
→ local DWG_todo scripts/operator_dwg_story_index_local.ps1
→ cad.build_story_index
```

本輪已準備的 promotion-grade 1F evidence：

- overview image：1200 × 800
- camera bounds：
  - min_x `44443.25117281456`
  - min_y `35301.305011156095`
  - max_x `117008.65481692566`
  - max_y `92030.00566073845`
- confirmed 1F world bounds：
  - min_x `44443.25117281456`
  - min_y `44319.71383237175`
  - max_x `58261.78081822565`
  - max_y `53992.684584159506`
- 依 OpenCAD 10px margin、uniform-fit、Y inversion deterministic 反算 pixel bounds：`[101, 533, 291, 666]`
- confidence：`0.97`
- unresolved：`2F / 3F / 4F / R1F`

注意：目前只允許 1F 進 Story Index；其餘樓層不得從名稱或排列猜座標。

完成驗收必須取得：

- local durable `work_id`
- claim/executor slot evidence
- `source_sha256`
- `overview_png`
- `story_index_manifest`
- `story_count`
- `story_index_png`
- `story_index_sha256`

目前尚未取得新版 local-work ACK，因此本步仍是 READY，不寫假完成。

### 0004-047 — Queue-owned per-story viewport render fanout

狀態：`PENDING`

依賴：0004-045 成功。

新版要求：

- 由 `:8848` 對每個已持久化 Story Index viewport 建立一個 durable child work。
- 最多四個 local executor 同時工作。
- 所有 child 先 submit，再由單一 OpenWorker coordinator 觀察／join。
- OpenWorker 不建立 business child jobs。
- GitHub Actions 不參與 fanout。

驗收：

- `story_job_ids`
- `render_manifest`
- 每層 PNG
- 每層 PNG SHA256
- rendered_story_count
- all required children terminal succeeded

已識別的 1F 必須至少產出 `1F.png`；未識別樓層不能製造空白假 PNG。

### 0004-049 — Multimodal Story Index + per-floor review join

狀態：`PENDING`

依賴：0004-047 join 完成。

要求：ChatGPT/多模態模型實際看 `story-index.png` 與每張樓層 zoom PNG；每層輸出 PASS / REWORK / UNRESOLVED。

驗收：review bundle、review receipt、story-index decision、per-story decisions、confirmed IDs、unresolved IDs。

### 0004-050 — Confirm real Story Regions

狀態：`PENDING`

依賴：0004-049。

每個 confirmed story 走：

```text
viewport world bounds
→ cad.query_bounds
→ cad.query_entities
→ label/geometry evidence
→ cad.set_story_region
```

不得以 Story Index 直接取代 Story Region semantic gate。

### 0004-055 — Column candidates visual confirmation

狀態：`PENDING`

每樓層列出候選柱 handle，產生可視 overlay，經視覺 review 後才建立 confirmed_column_handles。

### 0004-060 — Structural recognition + Story Design

狀態：`PENDING`

輸出必須含 `design_json`、`design_png`、SHA256、review decision。JSON 正確但圖片錯誤不可 PASS。

### 0004-070 — Reviewed materialization

狀態：`PENDING`

只有已 review 的結構設計可以 materialize；完成後必須 reopen/query 驗證實體 CAD 狀態。

### 0004-080 — Story anchors + registration + residual validation

狀態：`PENDING`

建立跨層共同 Building World Coordinate authority，保留 anchor receipt、registration receipt、residual validation。

### 0004-085 — AI-OpenSees structural authority validation

狀態：`PENDING`

使用最新 AI-OpenSees 正式 capability，驗證 structural model / building skeleton / MCT hash / node / element / result，不接受 placeholder。

### 0004-090 — Build Building World Coordinates

狀態：`PENDING`

輸出 Building World manifest + SHA256。

### 0004-100 — Export + validate production GLB

狀態：`PENDING`

輸出實體 GLB、size、SHA256、validation；不得只產 manifest。

### 0004-110 — Blender 5.2 REAL reopen + render

狀態：`PENDING`

Blender 必須真實 reopen GLB，記錄版本、mesh count、render path、render SHA256、reopen report。

### 0004-120 — Native AC1032 ACIS Solid3D DWG

狀態：`PENDING`

輸出實體 3D DWG、size、SHA256、Solid3D count、layer validation。

### 0004-130 — OpenCAD DwgReader second reopen validation

狀態：`PENDING`

對 0004-120 的實體 DWG 做第二次獨立 reopen / semantic validation。

### 0004-140 — Register artifacts in AI-Engineering-OS

狀態：`PENDING`

登記目前 canonical artifacts 與 revisions；不得用舊 artifact 冒充本輪。

### 0004-150 — Publish bounded Google Drive Review Bundle

狀態：`PENDING`

從 O87 本機將 review 所需的有限成果直接上傳 Google Drive。GitHub artifact 禁止作 review transport。

### 0004-160 — Apply ChatGPT semantic + visual review receipt

狀態：`PENDING`

只有實際 review receipt 可改 delivery gate；口頭推測不算 PASS。

### 0004-170 — Create approved Delivery Revision

狀態：`PENDING`

只有 0004-160 PASS 後才能建立 approved delivery revision。

### 0004-180 — Validate final delivery package + website

狀態：`PENDING`

最終檢查 package path、website path、artifact hashes、review provenance 與 final validation。

## 4. 本輪目前結論

截至本文件建立：

- Case 0004 已從舊控制器 checkpoint 遷移到新版 Go Case Engine revision 142。
- 010～040 的真實成果保留，沒有浪費時間重跑。
- 045 是唯一 canonical READY step。
- Go Bootstrap、Continue mapping、openworkerctl O87 config、Case0004 spec、永久測試均已補入 main。
- O87 部署 workflow 已改為安裝前跑 `go test ./...`。
- 本文件的 push 只觸發 O87 **程式升級**；案例業務步驟仍必須由 O87 本機 `openworkerctl → :8848` 啟動。
- 目前沒有新版 `0004-045` local-work ACK，因此不得宣告 Story Index 已完成。

下一個真實證據點固定為：

```text
O87 local supervisor REAL_VERIFIED + OPERATIONAL
→ openworkerctl case bootstrap 0004
→ openworkerctl case status 0004
→ openworkerctl case continue 0004
→ capture 0004-045 work_id / slot / receipt / story-index.png / SHA256
```

取得後必須立即追加到本文件，再進 0004-047 queue-owned parallel fanout。
