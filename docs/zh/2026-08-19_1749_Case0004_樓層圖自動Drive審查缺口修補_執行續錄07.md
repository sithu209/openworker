# Case 0004：樓層圖自動 Drive 審查缺口修補 — 執行續錄 07

時間：2026-08-19 17:49 +08:00（Asia/Taipei）

## 1. 缺口

此前 Case 0004 即使本機 Story viewport PNG 生成成功，成果仍可能只停留在 O87 workspace / durable receipt；ChatGPT 能看到 work_id、SHA、bounds、receipt，但不一定能直接讀到實際 PNG。

這違反 Case 0003 已驗證的成果審查原則：

`REAL artifact -> immutable review bundle -> Google Drive -> ChatGPT exact revision review -> PASS/TUNE/FAIL -> review receipt -> WorkLedger/Case`

因此「本機 PNG 已存在」不得等價於「ChatGPT 已看圖」。

## 2. 本批代碼修補

新增 `go-runtime/internal/casecontroller/story_review_publish_mapper.go`：

- 新 action：`openworker.review.publish-story-viewports`
- dispatch alias：`openworker.case.publish-artifacts`
- 只接受 terminal-success Story viewport fanout dependency；
- 強制收集 `render_manifest`；
- 強制收集非空 `story_pngs[]`；
- 每個 artifact 必須是 workspace 內真實存在的檔案；
- 同時帶入已驗證 overview PNG 作為視覺定位上下文；
- 產生 deterministic revision_id / work_code；
- 交由既有 `openworker.case.publish-artifacts` 建 immutable bundle、計 SHA、用 Google Drive API 發布；
- GitHub artifact transport 不參與。

新增測試：

- `story_review_publish_mapper_test.go`
- 驗證 exact artifact collection；
- 驗證 dispatch alias；
- 驗證 empty story_pngs fail-closed。

## 3. Case worklist revision 149

Case 0004 source definition 升到 revision 149。

Authority 明確拆分：

- `process_authority = openworker-go-case-engine/v1`
- `execution_authority = go-tool-runtime-local-supervisor`
- `queue_authority = go-tool-runtime:8848`
- fanout decision / plan / join owner = OpenWorker
- fanout queue / child execution owner = go-tool

新增步驟：

### 0004-0481

第三次 Story viewport fanout retry。

前兩次 047 / 048 的 REAL failure 不覆蓋、不刪除；0481 只在 O87 executor 已重新載入修復後 DWG authority 後執行。

### 0004-0485

`Automatically publish exact Story viewport PNGs and render manifest to bounded Google Drive review bundle`

依賴 0481 terminal-success。

Acceptance 至少要求：

- review_bundle
- manifest / manifest_sha256
- published_artifacts / published_artifact_sha256
- drive_receipt
- drive_folder_id
- drive_file_ids / drive_file_links
- chatgpt_review_ready
- transport

### 0004-049

只做 ChatGPT multimodal review gate，不再兼任成果發布。

必須審查 0485 exact Drive revision，回寫：

- llm_review_receipt
- story_index_decision
- per_story_decisions
- confirmed_story_ids
- unresolved_story_ids
- reviewed_drive_file_ids
- reviewed_manifest_sha256

沒有實際讀圖不得 PASS。

## 4. 正確閉環

```text
0481 REAL Story viewport PNG
  -> 0485 immutable bounded review bundle
  -> Google Drive API direct publish
  -> chatgpt_review_ready=true
  -> ChatGPT Drive connector 讀 exact file IDs
  -> 049 PASS / TUNE / FAIL
  -> receipt 綁 manifest SHA + reviewed file IDs
  -> PASS 才允許 050 cad.set_story_region
```

## 5. 本批 commits

- `4d32b16d1285c51d1bcf6a7fd41055815d2f888a` — Story viewport automatic Drive publish mapper
- `202af598d1469ca1455253db32524dacd8e6216d` — mapper fail-closed tests
- `a1f71898a5b7d0f23b7964e393cf1f5a6df28a6c` — Case0004 revision 149 + 0481/0485 review pipeline

## 6. 接續規則

1. 部署最新 OpenWorker 到 O87 resident `:8787`；
2. 由 OpenWorker refresh revision 149；
3. 不重跑舊 047/048 work_id；
4. 執行 0481；
5. 0481 成功後由 Case Engine 自動 dispatch 0485；
6. Drive receipt 出現後，ChatGPT 必須實際讀圖；
7. 未完成 049 視覺審查前，不得進 050。
