# CASE 0003 玉井橋：UL7 路由與 Drive 審查接續狀態

日期：2026-08-17（Asia/Taipei）

## 本輪確認

1. Case 0003 Final Acceptance 不再依賴自訂 `UL7` runner label。
2. workflow 改為 `runs-on: [self-hosted, Windows, X64]`，第一步以 `COMPUTERNAME == DESKTOP-UL7V2VV` fail-closed；錯機必須立即失敗，不得執行產品步驟。
3. 路由修正提交：`3c8e2b93d8c461f0e10653de0dc09be0865d5121`。
4. 路由永久回歸測試：`tests/test_case0003_ul7_routing_contract.py`，提交 `7b919307ef446c512403b8836dce10c5cdb21a1c`。
5. 最新正式 Final Acceptance run：`31991159862`。目前仍在等待 self-hosted Windows runner 接單；不得另開競跑。

## Google Drive 審查鏈確認

固定交換資料夾：`OpenWorker-ChatGPT-Review-TEMP`。

固定 folder id：`1A4BnZEcFe2WIhcperRd4QSpxoSUN_ARR`。

`coworker/review_cycle.py` 已具備：

- Review Bundle 建立；
- 每個 artifact SHA256；
- manifest / review-request；
- Google Drive Desktop 同步根目錄解析；
- staging copy；
- 全樹檔名與 SHA256 一致性驗證；
- atomic rename 到正式 revision 目錄；
- LLM receipt 套用；
- PASS / TUNE / FAIL 治理；
- TUNE 僅允許 allowlist parameter；
- FAIL 建立返工要求。

`case0003_review_handoff.py` 的 review bundle 至少包含：

- Blender `terrain-render.png`；
- SceneX `terrain-browse.png`；
- SceneX REAL evidence JSON；
- Delivery website index；
- mechanical acceptance state。

## 尚未允許宣告完成的最後條件

Google Drive Desktop 本機 copy 成功，不等於 Google Drive 雲端端已可被 ChatGPT Connector 讀取。

因此 Case 0003 的最終語意閉環必須是：

`REAL workspace artifacts`
→ `mechanical acceptance`
→ `immutable Review Bundle`
→ `Google Drive Desktop sync folder`
→ `Google Drive cloud connector 能搜尋/讀回同 revision`
→ `ChatGPT 實際讀 Blender / SceneX / Delivery 成果`
→ `llm-review-receipt.json`
→ `receipt apply`
→ `WorkLedger accepted/delivered pointer`

在 Connector 尚未讀到該 revision 之前，只能標記 `WAITING_CLOUD_VISIBILITY` 或 `WAITING_LLM_REVIEW`，不可宣告 semantic review 已完成。

## 目前唯一外部 blocker

最新 run `31991159862` 尚未被任何 self-hosted Windows runner 接走。路由 workflow 已經不要求 `UL7` 自訂 label，所以目前 blocker 是 runner availability / runner service，而不是 Case 0003 workflow label。

本輪不重跑 DTM、AOI、Consumer、Blender、既有 SceneX 成功成果；只等待 Final Acceptance 新鮮 reopen / screenshot / Drive handoff。
