# Case 0004：O87 本機總控 8848 修復與 REAL 4+4 驗證 — 執行續錄 05

- 更新時間：2026-08-19 15:12 +08:00
- Case：`0004`
- 固定機：`DESKTOP-O87PJNR`
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`

## 1. 本輪 REAL control evidence

ChatGPT 已透過 bounded PR transport 直接命中 O87 self-hosted runner：

```text
PR #27
Dispatch Local Supervisor Command O87
run_id = 32226331620
job_id = 95986647873
runner  = DESKTOP-O87PJNR-R003
machine = DESKTOP-O87PJNR
```

GitHub Action 只承擔 transient command transport：

```text
github_action_used_for_command_transport = true
github_action_used_for_business_execution = false
authoritative_business_state = openworker
```

## 2. 第一個缺口已修：OpenWorker CLI 未安裝

前一輪 PR #26 的 REAL receipt 顯示：

```text
exit_code = 127
OpenWorker control executable is not installed
```

已修 `scripts/run-o87-transient-command.ps1`：當 `%ProgramData%\OpenWorker\bin\openworker.exe` / `openworkerctl.exe` 缺失時，從目前 `main` 的 Go source 建置並安裝最新版 CLI，再執行 bounded local command。

修復 commit：

```text
801990f9d69bb5f3f5ad421e6b79dd53efac6164
```

## 3. 第二個 REAL 缺口：127.0.0.1:8848 未監聽

PR #27 已成功越過 CLI 缺失，真正執行：

```text
openworker.exe case status 0004
```

但 immutable receipt 返回：

```text
request_id = pr-27-32226331620
accepted   = false
exit_code  = 1
error      = dial tcp 127.0.0.1:8848: connectex: No connection could be made because the target machine actively refused it
```

這證明：

- O87 runner 在線；
- ChatGPT -> O87 bounded control transport 已證明可達；
- OpenWorker Go CLI 已可執行；
- 真正阻塞點是 O87 的 go-tool-runtime authoritative local supervisor 未啟動/未監聽 `:8848`；
- 此錯誤不是 Case0004 business failure。

## 4. 直接復用 go-tool-runtime 既有 authority

`liuxb99/go-tool-runtime` 已存在：

```text
.github/workflows/case0004-o87-maintenance.yml
```

其 canonical 維護流程為：

```text
固定 O87 hostname 驗證
-> install-gtr-local-work-runtime.ps1
-> 127.0.0.1:8848/health
-> one-call clear authoritative O87 queue
-> verify queue empty
-> verify-gtr-true-local-supervisor.ps1
-> REAL_VERIFIED / LOCAL_SUPERVISOR
-> >=4 claim workers
-> >=4 executor slots
-> github_action_used_for_business_execution=false
-> durable maintenance receipt
```

本輪沒有另造一套安裝器。

## 5. 新增 ChatGPT bounded maintenance trigger

已讓上述 workflow 支援 exact-title PR trigger：

```text
[go-tool-control] case0004_o87_repair
```

且只允許 same-repository PR；job 仍固定：

```text
runs-on: [self-hosted, Windows, X64, O87]
```

修改 commit：

```text
b0a476e59c09855b5a10ef49b76af1d25495f04f
```

已建立 go-tool-runtime PR #5：

```text
head = control/case0004-o87-repair-20260819-1511
marker commit = 34a1c3129980745347d99acfd36f5d512ce59d4d
```

对应 maintenance run：

```text
run_id = 32226533459
current status = QUEUED
```

目前不得把 QUEUED 寫成修復完成。

## 6. 下一個 authority gate

只有 `32226533459` 真正完成並產生：

```text
status = REAL_VERIFIED
route_label = LOCAL_SUPERVISOR
distinct_claim_worker_count >= 4
distinct_executor_slot_count >= 4
github_action_used_for_business_execution = false
```

才重新發：

```text
openworker case status 0004
```

若 status 證明 045 READY / supervisor OPERATIONAL，才發 `case continue 0004`。

## 7. Case0003 成果審查原則保持不變

後續 045/047 REAL Story Index / per-story PNG 產出後：

```text
REAL artifacts
-> immutable review bundle
-> drive.review.publish
-> Google Drive exact revision
-> ChatGPT 實際 multimodal inspection
-> PASS / TUNE / FAIL
-> review receipt
-> WorkLedger / Case acceptance
```

機械 receipt 不得取代 ChatGPT 視覺品質 gate。
