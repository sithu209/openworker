# Case 0004：O87 直達 PR 控制通道 — 執行續錄 04

- 更新時間：2026-08-19 15:05 +08:00
- Case：`0004`
- 固定機：`DESKTOP-O87PJNR`（O87）
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 承接：`docs/zh/2026-08-19_1458_Case0004_ChatGPT本機總控與Case0003成果審查閉環_執行續錄03.md`

## 1. 本輪 REAL 發現

PR #25 已成功造成 `OpenWorker Control PR` workflow event，但 run `32225470094` / job `95984183428` 長時間停在 `queued`。

原路徑為：

```text
ChatGPT PR
-> ubuntu-latest openworker-control-pr.yml
-> workflow_dispatch
-> O87 self-hosted runner
-> openworker.exe
```

其中 `ubuntu-latest` 只做短命令轉發，沒有任何 business authority，卻成為新的等待點。

因此不能把 PR #25 queued 誤判為 O87 已接單，也不能在沒有 O87 `final.json` 的情況下提交 `case_continue`。

## 2. 已修正：PR event 直接排到 O87

更新：

```text
.github/workflows/dispatch-local-supervisor-command-o87.yml
commit = b902069adfd8d16e50a9f9ed6d9198512c642ede
```

新增 `pull_request: opened` 入口，只有下列同 repo bounded control PR 可進入：

```text
[openworker-control] case0004_status
[openworker-control] case0004_continue
```

並要求 PR body 明確包含：

```text
requested_by: ChatGPT
case_id: 0004
machine: DESKTOP-O87PJNR
```

新的 direct path：

```text
ChatGPT opens bounded PR
-> pull_request event
-> [self-hosted, Windows, X64, O87]
-> scripts/run-o87-transient-command.ps1
-> %ProgramData%\OpenWorker\bin\openworker.exe
-> case status/continue 0004
-> immutable command-results/o87/<request>/final.json
```

GitHub Action 仍只做 command transport；`run-o87-transient-command.ps1` receipt 固定：

```text
github_action_used_for_command_transport = true
github_action_used_for_business_execution = false
business_completion_claimed = false
authoritative_business_state = openworker
```

## 3. 新 REAL status control request

建立分支：

```text
control/case0004-status-direct-20260819-1501
```

marker commit：

```text
b40a351a5c66bd7802867fe9418d64c1d46c3a90
```

建立：

```text
PR #26
Title = [openworker-control] case0004_status
Base  = main
Head  = control/case0004-status-direct-20260819-1501
```

目的仅为读取 O87 上 canonical：

```text
openworker.exe case status 0004
```

截至本文件更新时：

```text
PR #26 = OPEN
workflow run receipt = 尚未返回
PR comment = 尚无
O87 final.json = 尚未证明存在
```

因此当前判定仍为：

```text
O87 accepted status command = NOT YET PROVEN
Case 0004 business progress = 不因 transport 等待而重提/猜测
```

## 4. Case 0003 视觉审查约束继续适用

Case 0003 已证明：

```text
workflow success + file exists + non-empty PNG + scene reopen
!= visual result PASS
```

所以 Case 0004 后续必须维持：

```text
REAL Story Index / story PNG / 3D render
-> immutable review bundle
-> Google Drive publish receipt
-> ChatGPT 读取 exact Drive revision
-> ChatGPT visual PASS / TUNE / FAIL
-> review receipt
-> WorkLedger / Case gate
```

没有 ChatGPT 实际读取视觉成果，不得用机械 evidence 代替 perceptual acceptance。

## 5. 下一步 authority 顺序

1. 只读取 PR #26 / O87 `final.json`；
2. 若 `case status 0004` 显示 045 READY 且 runtime operational，才创建新的 bounded `case0004_continue` PR；
3. `case_continue` 只确认 durable work identity / queue accepted，不等待 business completion；
4. 再用 `case_status` 读取 terminal reconciliation；
5. 045 完成后由 generic Go Case Engine 进入 047 Story fanout；
6. 取得真实 story PNG/hash 后发布 Drive review bundle；
7. ChatGPT 实际审查后才决定是否进入后续结构识别/3D。
