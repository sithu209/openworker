# 真正本機總控：實作完成，等待 ODA 實機驗證

日期：2026-08-19

## 狀態

```text
SOURCE IMPLEMENTED
REAL LOCAL VERIFICATION PENDING
```

本批已完成「真正本機總控」所需的核心源碼閉環；但在 ODA 實際安裝、看到 4-slot 同時工作、完整 durable ledger 與 Case 實體成果之前，不得宣告 REAL VERIFIED。

## 最終權威架構

```text
Case controller
  coworker.case0005_local_supervisor
        |
        v
OpenWorker :8787
  durable child process jobs
        |
        v
gtr-local-exec compatibility client
        |
        v
go-tool-runtime :8848
  durable local-work queue
        |
        +-- claim slot 01
        +-- claim slot 02
        +-- claim slot 03
        +-- claim slot 04
        |
        +-- execution slot 01
        +-- execution slot 02
        +-- execution slot 03
        +-- execution slot 04
        |
        v
allowlisted local capability
```

權威分工：

- `go-tool-runtime :8848`：本機 Action queue / parallel scheduler / tool authority。
- `OpenWorker :8787`：Case DAG、process job、PID、timeout、cancel、execution explain。
- `coworker.case0005_local_supervisor`：Case 0005 orchestration authority。
- GitHub Actions：不得執行 Case business work；只允許 code update / repair / CI 等非業務用途。

## 已完成

### 1. 4-slot 真正並行

- gtr-work-agent：4 claim workers。
- gtr-work-executor：4 execution workers。
- 每個 slot 有獨立 worker id / lease / heartbeat。
- executor 使用 execution lock 防止同一 work 重複執行。
- execution lock 有 heartbeat，crash 後可 stale recovery。

### 2. Case 不再繞過 :8848

`gtr-local-exec` 已改成 durable local supervisor compatibility client：

```text
gtr-local-exec
 -> POST :8848/api/execution/local-work
 -> wait durable terminal
```

它不再自己直接執行 capability。

### 3. 雙層 durable ledger

Case 層：

```text
D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\case-supervisor-ledger.jsonl
```

Action 層：

```text
runtime_work_items
runtime_work_events
```

Action ledger 可還原：

```text
submitted
claimed
execution_started
heartbeat
execution_finished / execution_failed
completed / failed
lease_reclaimed
```

並記錄 claim slot、executor slot、PID、attempt、duration。

### 4. 0005 IMAGE / VIDEO 真 fan-out

- `0005-030`：一個角色 asset = 一個 durable local work。
- `0005-040`：一個場景 asset = 一個 durable local work。
- `0005-060`：一個 approved shot = 一個 durable local work。

最終 controller：

```text
coworker.case0005_local_supervisor
```

所有 generic / image / video child 都重新進入此同一 module，避免 controller authority 中途漂移。

### 5. Worklist authority 已統一

Case 0005 worklist revision 5：

```text
parallel_policy.authority = go-tool-runtime-local-supervisor
max_local_slots = 4
required_execution_route = local_supervisor
process_kernel = openworker-local-supervisor
```

### 6. Execution Route 權威查詢

```text
GET /api/execution/route?id=...
```

明確返回：

```text
LOCAL_SUPERVISOR
GITHUB_ACTIONS
UNKNOWN
```

本查詢本身不呼叫 GitHub、不觸發 workflow。

Case 0005 business work 必須為：

```text
route_label = LOCAL_SUPERVISOR
github_action_used_for_business_execution = false
```

### 7. Case runtime 單一查詢

```text
GET /api/openworker/case/runtime?case_id=0005&machine=DESKTOP-ODAQN0D
```

可直接回答：

- machine_active_slots
- machine_free_slots
- case_active_slots
- case_queued
- case_inflight
- Case work IDs
- Case event ledger
- LOCAL route 是否已有 durable evidence

### 8. 四 binary 本機 self-update

本機 self-update scope：

```text
tool-runtime.exe
gtr-work-agent.exe
gtr-work-executor.exe
gtr-local-exec.exe
```

不是只更新 server。

### 9. 無副作用 4-slot smoke capability

Capability：

```text
go-tool.local-supervisor.smoke
```

只做：

- COMPUTERNAME 驗證
- bounded sleep
- timing receipt

不跑 shell、不碰 business artifact、不用 GitHub。

### 10. ODA 真實 4-slot 自驗證腳本

```text
scripts/windows/verify-gtr-local-supervisor.ps1
```

它會直接對 `127.0.0.1:8848` 提交 5 個 smoke work，要求實際觀察：

```text
peak_active_slots = 4
free_slots = 0
pending >= 1
```

並要求：

- 5 work 全部 completed。
- 每個 route 都是 LOCAL_SUPERVISOR。
- 至少 4 個不同 claim worker IDs。
- 至少 4 個不同 executor slots。
- 每個 work 有 submitted/claimed/execution_started/execution_finished/completed ledger。

驗證 receipt：

```text
%ProgramData%\go-tool-runtime\work-agent\local-supervisor-verification.json
```

### 11. Case 0005 一鍵真正本機啟動

```text
scripts/activate-case0005-local-supervisor.ps1
```

此腳本在 ODA 本機執行：

1. 驗證 COMPUTERNAME = DESKTOP-ODAQN0D。
2. 本機 `git pull --ff-only` 同步程式碼；這只是 code update，不是 GitHub Actions business execution。
3. 安裝四 binary local supervisor，workers=4。
4. 執行 5-work / 4-slot smoke verification。
5. 直接 POST `127.0.0.1:8848/api/openworker/case/bootstrap`。
6. controller 固定 `coworker.case0005_local_supervisor`。
7. 等第一個 Case local-work 出現。
8. 要求 Case runtime route = LOCAL_SUPERVISOR。
9. 寫入：

```text
D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\true-local-supervisor-activation.json
```

整個 Case bootstrap/status/business 路徑不 dispatch GitHub workflow。

## ODA 最終驗收門檻

真正成功必須看到實機證據：

1. `local-queue-authority.json`：claim_workers=4、execution_workers=4。
2. `local-supervisor-verification.json`：status=VERIFIED、peak_active_slots=4。
3. `true-local-supervisor-activation.json`：controller=`coworker.case0005_local_supervisor`。
4. Case business work `/api/execution/route` 全部 LOCAL_SUPERVISOR。
5. `/api/openworker/case/runtime` 可看到 Case 佔用的本機 slots。
6. `runtime_work_events` 有不同 claim/executor slots 與 PID。
7. Case supervisor JSONL ledger 可完整重放。
8. 0005 實體 PPTX / images / MP4 與 SHA256 / QC 對得起來。
9. 正常 Case business execution 期間沒有 GitHub Actions business route。

全部滿足後，狀態才能改成：

```text
REAL LOCAL SUPERVISOR VERIFIED
```
