# Case 0004：Go 本機總控＋並行 Action 執行續錄 02

時間：2026-08-19 14:37 +08:00  
Case：`0004`  
固定主機：`DESKTOP-O87PJNR`（O87）  
Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`  
本機總控：`go-tool-runtime :8848`  
OpenWorker process kernel：`:8787`  
Worklist：revision `144`

## 1. 本輪開始時的真實狀態

- 0004-010：PASSED / RECONCILED
- 0004-020：PASSED / RECONCILED
- 0004-030：PASSED / RECONCILED
- 0004-040：PASSED / RECONCILED
- 0004-045：READY
- 0004-047～180：PENDING

本輪沒有重跑 010～040。

## 2. O87 DWG authority 已確認落地

O87 最新可查的 DWG_todo authority receipt：

- authority：`dwg-todo`
- assigned_host：`DESKTOP-O87PJNR`
- commit：`a0f884fc8b1e6b68dc57bafdb4da54fb381032c4`
- root：`C:\ProgramData\go-tool-runtime\work-agent\authorities\dwg-todo-a0f884fc8b1e`
- installed_at：`2026-08-19T06:13:12.7011203Z`

因此 O87 本機 authority 已包含前一輪補入的 `cad.build_story_index` 與 `cad.render_story_viewports` canonical wrapper allowlist。

## 3. 外部 ChatGPT → O87 控制面檢查

本輪嘗試從目前 ChatGPT 執行環境直接解析：

`DESKTOP-O87PJNR:8848`

結果：此執行環境沒有加入使用者的 LAN/Tailscale DNS，hostname 無法解析，因此不能直接由本環境 HTTP 呼叫 O87 本機 queue。

這個結果只代表「目前 ChatGPT tool runtime 沒有 O87 私網路由」，**不代表 O87 offline**。既有 repo evidence 仍顯示 O87 `:8848` queue 與 4×4 local supervisor 已被 REAL 驗證。

依本 Case 硬規則，本輪沒有改用 GitHub Action 代替 `0004-045` business execution。

### 控制面 blocker 編號：GOC4-013

名稱：`ChatGPT runtime cannot directly reach O87 localhost/Tailscale queue`

禁止的 workaround：

- GitHub Actions business execution
- GitHub Actions status transport
- GitHub artifact 作 Case evidence transport

允許方向：

- 已存在且受控的 Remote MCP / secure tunnel / Tailscale control endpoint
- 或其他直接抵達 O87 Go local supervisor 的 authenticated control ingress

在該入口沒有被目前 ChatGPT tool runtime 接通前，0004-045 不能由本對話直接發送真實 local-work。

## 4. 0004-047 並行 Action 缺口補齊

雖然 045 尚待真實 dispatch，本輪先把 045 成功後一定會遇到的 047 缺口補掉，避免案例在下一步再次停住。

### GOC4-014 — 單一 fanout step 不會觸發 generic fanout

既有 generic fanout runtime 原本只在同時出現多個 READY fanout parent step 時觸發，適合 Case0005 多角色圖片 fanout。

Case0004 的 047 結構不同：

- 只有一個 READY parent step：`0004-047`
- child 數量由 045 `stories[]` 動態決定
- 每個 story_id 必須是一個 durable local-work

修復：新增 `case0004_story_fanout.go`。

行為：

```text
0004-045 evidence.stories[]
→ 逐 story_id
→ deterministic child work_id
→ capability = dwg.story_index.execute.case-worklist
→ method = cad.render_story_viewports
→ params.story_ids = [單一 story_id]
→ submit all children to :8848
```

提交：`946c4b6d932914e3e559cb172442cf0b4b144b75`

### GOC4-015 — Continue 只會啟動多 parent fanout

修復：`continue.go` 新增 single-step DWG fanout branch。

當唯一 READY step 同時符合：

- `kind=fanout`
- `evidence_profile=dwg_story_viewports`

就直接：

1. 建立 fanout plan。
2. 一次 submit 所有 child durable work。
3. 持久化 `.openworker/case-fanout-last.json`。
4. controller 回報 `fanout_active`。
5. 下一次 continue 先 reconcile children，再決定是否 join 成功。

提交：`bb92a65a9e150acc98a90743aeca75891bd5d9f0`

### GOC4-016 — generic fanout join 只認 ComfyX receipt.data

既有 join 只會聚合：

- workspace_relpath
- workspace_artifact.sha256

不適用 DWG Story viewport receipt。

修復：`fanout_runtime.go` 新增 `dwg_story_viewports` evidence profile。

每個 child 必須提供：

- `_fanout_work_id`
- `manifest_path`
- `stories[]`
- `stories[].png_path`
- `stories[].png_sha256`
- `stories[].source_dwg_sha256`

join 後生成 047 acceptance：

- `source_sha256`
- `render_manifest`
- `story_job_ids`
- `rendered_story_count`
- `story_pngs`
- `story_png_sha256s`
- `all_required_stories_terminal_succeeded=true`

任一 child：

- pending / claimed / running → 047 保持 fanout_active
- failed → 047 FAILED + blocker
- source hash 不一致 → join fail-closed
- 缺 PNG / SHA256 / manifest → join fail-closed

提交：`746e716f09b8f97086bb06c7ee9c45666fd8abf4`

### GOC4-017 — Worklist 缺少 typed fanout metadata

Worklist 升到 revision 144。

`0004-047` 新增：

- `evidence_profile=dwg_story_viewports`
- `fanout_role=story_viewport`
- `fanout_evidence_prefix=story_render`

並保留 queue-owned fanout、4-slot executor、GitHub business execution forbidden 等既有政策。

提交：`2f60652c4cff172fd08496253e90db5110c2b641`

該提交帶 `[bootstrap-o87-node]`，只用來更新 O87 OpenWorker code，不執行 business step。

### GOC4-018 — Fanout regression tests

新增：`case0004_story_fanout_test.go`

測試內容：

1. 045 evidence 有 `2F/1F` 時能產生兩個 durable child。
2. child capability 固定為 `dwg.story_index.execute.case-worklist`。
3. method 固定為 `cad.render_story_viewports`。
4. 兩個 child evidence 可聚合成 count=2。
5. source hash 必須一致。
6. terminal success evidence 必須為 true。

提交：`b7bf9e04df78bf5cd77dcc46d11e001eee436c8c`

## 5. Step-by-step business ledger 增量

### 0004-010

狀態：PASSED / RECONCILED。

本輪無 business dispatch。

### 0004-020

狀態：PASSED / RECONCILED。

本輪無 business dispatch。

### 0004-030

狀態：PASSED / RECONCILED。

本輪無 business dispatch。

### 0004-040

狀態：PASSED / RECONCILED。

本輪無 business dispatch。

### 0004-045

狀態：READY。

本輪已確認：

- O87 DWG authority 已更新。
- go-tool local executor 具備 Story Index alias / evidence flatten。
- Case worklist 已升 revision 144。

本輪未取得：

- 045 local durable work_id
- claimed_by
- executor_slot
- execution_started / finished
- 045 acceptance evidence

原因：目前 ChatGPT tool runtime 無法直達 O87 private control endpoint；依政策不使用 GitHub business Action 代跑。

因此 045 **仍維持 READY**，不寫假完成。

### 0004-047

狀態：PENDING，但程式路線已補齊。

045 一旦 SUCCEEDED，047 會：

```text
stories[]
→ N 個 :8848 durable child
→ 最多 4 executor slots 並行
→ child receipts
→ Go fanout join
→ 047 acceptance
```

目前只有 promotion-grade 1F 時，fanout 只有 1 個 child 是正常結果；不會為了顯示「四並行」製造 2F/3F/4F/R1F 假任務。

### 0004-049～0004-180

仍全部 PENDING。

沒有因程式碼完成而改寫任何 business status。

## 6. 本輪結論

本輪完成：

1. 確認 O87 最新 DWG local authority 已安裝。
2. 確認目前 ChatGPT runtime 無法直接路由至 O87 私網控制面。
3. 不違反規則、不使用 GitHub Action 代跑 business step。
4. 補齊 047 單 parent → 多 story durable child 真並行架構。
5. 補齊 DWG fanout evidence join。
6. Worklist 升 revision 144。
7. 增加永久回歸測試。
8. 觸發 O87 code deployment only。

下一個不可替代的真實 Case evidence 仍是：

```text
O87 local controller reachable
→ refresh/bootstrap revision 144
→ continue 0004
→ 0004-045 durable ACK
→ executor receipt
→ reconcile 045 SUCCEEDED
→ automatically dispatch 0004-047 story fanout
```
