# Case 0004：O87 控制面修复与 Story Index 能力注册 — 执行续录 05

- 更新时间：2026-08-19 15:30 +08:00
- Case：`0004`
- 固定机器：`DESKTOP-O87PJNR`
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 当前原则：GitHub Actions 只做短命令/部署传输；OpenWorker + go-tool-runtime 才是业务 authority；视觉成果必须走 Google Drive → ChatGPT 实体审查。

## 1. 本轮已确认的 REAL 状态

1. ChatGPT → PR → O87 self-hosted runner 的 bounded command transport 已证实可达。
2. O87 `127.0.0.1:8848` 已重新安装并健康。
3. REAL local supervisor verifier 已产生：`REAL_VERIFIED`、`OPERATIONAL`、4 distinct claim workers、4 distinct executor slots，且 `github_action_used_for_business_execution=false`。
4. O87 resident OpenWorker 已升级并验证：`running_commit=target_commit=8090ebd112e19e39b43a2b8f7dc1af297359a6af`，`upgrade_verified=true`，`max_workers=4`。
5. Case status REAL receipt 已确认：workspace revision 146，当时 canonical next step 为 `0004-045`，状态 READY，`0004-047` PENDING。

## 2. Continue 失败链与已修复缺口

### 2.1 旧 resident OpenWorker 不认识 generic Case payload

失败：`json: unknown field "manifest_path"`。

处理：升级 O87 resident OpenWorker 到当前 generic manifest-driven Case API。

### 2.2 Source definition revision 落后 workspace

失败：`definition downgrade forbidden current=146 source=144`。

处理：先把 source revision 对齐到 146，不改 PASSED evidence / status。

### 2.3 同 revision 内有结构变化

失败：`step 0004-047 structural change requires newer revision`。

处理：将 typed Story fanout definition 提升为 revision 147。此次 revision 只表达 047 结构变化，不篡改历史状态。

### 2.4 Definition refresh 已成功，当前 blocker 缩小到 DWG capability registry

PR #35 / run `32227833648` 已返回：

- `definition_refresh.previous_revision = 146`
- `definition_refresh.revision = 147`
- `definition_changed = true`
- `updated_step_ids = ["0004-047"]`

因此 local workspace 已合法接收 revision 147 的 typed Story viewport fanout 定义。

随后 045 提交被 go-tool local capability preflight 拒绝：

`capability_id = dwg.story_index.execute.case-worklist`

`capability_supported = false`

理由：installed local executor contract 未注册该 capability。

## 3. 根因

`gtr-work-executor` 源码本身已经支持：

- `-dwg-todo-root`
- `DWG_TODO_ROOT`
- `%ProgramData%\OpenWorker\machine-roots.json`
- `%ProgramData%\go-tool-runtime\work-agent\authorities\dwg-todo-current.json`
- alias `dwg.story_index.execute.case-worklist`

所以不是能力代码缺失，而是当前 O87 executor 启动时没有取得有效 DWG local authority root。

## 4. 当前正在执行的修复

DWG_todo 已增加 bounded O87 authority bootstrap PR 入口。

- Repo：`liuxb99/DWG_todo`
- workflow：`.github/workflows/bootstrap-o87-local-authority.yml`
- workflow trigger 增补 commit：`a16553d2f49ecd72a79c82599814b2bd70b120b7`
- PR：`#1 [dwg-control] bootstrap_o87_authority`
- run：`32228019453`
- job：`95991702891`
- 当前已通过 fixed hostname 与 checkout，正在 `Snapshot tracked DWG authority`。

目标是重建：

`%ProgramData%\go-tool-runtime\work-agent\authorities\dwg-todo-current.json`

并使其指向含 `go.mod` / `cmd\dwg-editor\main.go` / `scripts\invoke-agent-cad-local.ps1` 的不可变 DWG authority snapshot。

完成后必须重启/重装 resident gtr-work-executor，使 `dwg.story_index.execute.case-worklist` 出现在 registered capabilities，再重发 `openworker case continue 0004`。

## 5. 后续固定闭环

```text
DWG authority current pointer
→ restart/reinstall gtr-work-executor
→ capability preflight sees dwg.story_index.execute.case-worklist
→ openworker case continue 0004
→ 0004-045 durable work_id accepted
→ reconcile terminal state
→ 0004-047 durable per-story fanout
→ REAL Story PNG + SHA256
→ 0004-049 Drive publish
→ ChatGPT reads exact Drive revision and visually checks Story Index + every rendered floor
→ PASS/TUNE/FAIL receipt
→ PASS only → 0004-050
```

不得把 workflow queued/success、文件存在或 PNG 非空代替 ChatGPT 的视觉验收。
