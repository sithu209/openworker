# Case 0004 — Story Index 主線切換成功與圖片取回阻塞

日期：2026-08-18
Case：0004
Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
固定主機：`DESKTOP-O87PJNR`

## 1. 本輪真實狀態

O87 最新 governed Worklist state 已成功套用 revision 4 新主線：

- REAL state run：`32093991410`
- machine：`DESKTOP-O87PJNR`
- runner：`DESKTOP-O87PJNR-R004`
- durable revision：`141`
- canonical next step：`0004-045`
- `0004-010`～`0004-040`：保留原 REAL `PASSED`
- `0004-045`：`READY`
- `0004-047 / 049 / 050`：依序 `PENDING`

這證明 `case_worklist_sync_manifest.py` 修復已生效；舊的 `R-0004-050-002` 不再是 canonical next step。

## 2. 新治理主線

```text
0004-040 overview
→ 0004-045 cad.build_story_index
→ 0004-047 cad.render_story_viewports
→ 0004-049 multimodal verify Story Index + every floor PNG
→ 0004-050 cad.set_story_region for visually accepted stories
```

不得退回「固定切圖後直接猜單一 Story Region」的舊主線。

## 3. 工具與 go-tool 已對齊

正式 go-tool capability：

`dwg.story_index.execute.case-worklist`

對應：

- `0004-045` → `cad.build_story_index`
- `0004-047` → `cad.render_story_viewports`

operator：`DWG_todo/.github/workflows/operator-dwg-story-index.yml`

固定機器 authority 為 persisted `assigned_host` 與 exact `COMPUTERNAME` equality，不依賴 custom O87 label。

## 4. 目前唯一主 blocker：實體 overview 圖片取回

`0004-045` 的 LLM viewport localization 必須建立在大模型實際看過完整 `case0004-overview.png` 的前提下。

已知 overview authority：

- workspace path：`D:\AI-Work\jobs\0004-DWG-TO-3D\dwg\exports\default\visual-search\case0004-overview.png`
- SHA256：`5cee03340cbbcad51e412b46b85bda9dcaac22b193586b953bbfd5134039103e`
- source DWG SHA256：`aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6`

private GitHub repo 裡已有 base64 備份，但文字型 base64 不能當作多模態審查 authority。

因此新增實體 PNG artifact handoff workflow：

`.github/workflows/case-0004-o87-overview-artifact-handoff.yml`

commit：`51f2eb49df5c24179d017fc672bb3fe2c67c0406`

它會：

1. exact route 到 `DESKTOP-O87PJNR`；
2. 驗證 overview 實體存在且 SHA 必須等於 `5cee0334...`；
3. 上傳 `case0004-overview.png` 為 GitHub Actions artifact；
4. 回寫 `review-tmp/case0004/latest-overview-artifact-handoff.json`，包含 run id / machine / runner / size / SHA。

截至本次記錄，該 receipt 尚未回寫，因此尚未宣告 artifact handoff 成功。

## 5. Fail-closed 原則

在 ChatGPT 真正取得並觀看完整 overview 前：

- 不得硬填 `2F / 3F / 4F / R1F` 的 pixel bounds；
- 不得只沿用舊 `c-mid-left` 推定全部樓層；
- 不得執行假的 `cad.build_story_index`；
- 不得宣告 `0004-045` PASS。

## 6. 下一個驗收點

下一個合法驗收：

```text
latest-overview-artifact-handoff.json 出現
→ 用 run id 取回實體 overview PNG
→ ChatGPT 看完整總圖並輸出多樓層 pixel bounds + confidence + visual basis
→ 0004-045 REAL cad.build_story_index
→ 產生 story-index CAD state + annotated story-index.png
→ 0004-047 逐層 REAL zoom PNG
→ 0004-049 逐張多模態檢核
```

目前不是樓層識別算法本身卡住，而是「多模態模型尚未取得完整實體 overview」的 transport blocker。