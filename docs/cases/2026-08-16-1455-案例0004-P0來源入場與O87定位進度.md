# 2026-08-16 14:55｜案例 0004：P0 來源入場與 O87 定位 REAL 進度

> 主手冊：`docs/cases/0004-DWG轉3D-REAL完整閉環手冊.md`
>
> Action-only 實施手冊：`docs/cases/2026-08-16-1435-案例0004-DWG轉3D-ActionOnly成果閉環實施手冊.md`
>
> 狀態：`P0 IMPLEMENTING / O87 REAL ROUTE VERIFIED / PRIMARY SOURCE BYTES NOT YET MATERIALIZED`

---

## 1. 本批目的

P0 只處理一件事：把使用者本次上傳的真實 primary DWG，在不公開、不手工假閉環、不改內容的前提下，正式送進 O87 上的 OpenWorker / AI-Engineering-OS canonical Job workspace。

Primary source authority：

```text
原始檔名：S1-1140926(1).dwg
size：1,385,583 bytes
SHA256：aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6
header：AC1032
```

目標 canonical path：

```text
D:\AI-Work\jobs\0004-DWG-TO-3D\input\source.dwg
```

指定 host：

```text
DESKTOP-O87PJNR
```

---

## 2. P0 production source-ingress 能力已建立

OpenWorker 新增可重用 production module：

```text
coworker/engineering/source_ingress.py
```

Commit：

```text
3109af8c028cf4b86e053ac927e0c7098d8e1474
```

能力不是 0004 hardcode，正式語意：

```text
fixed-host gate
→ ensure AI-Engineering-OS Project / Job scope
→ create/reuse OpenWorker JobBinding
→ source size / SHA256 / header gate
→ atomic canonical source materialization
→ OS Job Input import
→ OS input physical SHA verification
→ OS Artifact Registry source-dwg registration
→ input/source-provenance.json
→ OpenWorker ProjectKnowledge accepted event
```

Exact retry：

- canonical source 同 SHA → 不覆寫；
- OS Job Input 同 original_name / size / SHA → 不重複 import；
- OS source-dwg artifact 同 job/component/kind/SHA → 不重複 register；
- canonical path 已存在不同 SHA → fail closed。

永久測試：

```text
tests/test_engineering_source_ingress.py
```

Commit：

```text
412fd350710a21bac8d9cfbeb55c96c5aaf52da6
```

目前尚不可寫 `REAL PASS`，因為這批測試仍需跟正式 O87 ingress Action 一起跑到 terminal。

---

## 3. Source transport 設計

OpenWorker 是 public repo，因此真實使用者 DWG **不得** commit 到 OpenWorker。

AI-Engineering-OS 是 private repo，因此正式 transport 設計可使用 private immutable Git blob；OpenWorker ingress driver 已支援：

```text
single private Git blob
或 source_blob_shas[] 分塊 blob
+ optional XZ compression
+ packed size/SHA256
+ decompressed original size/SHA256/header
```

Action driver：

```text
scripts/engineering_source_ingress_action.py
```

主要 commits：

```text
1400591e...  initial Action driver
81e6549e...  chunked private source transport
```

第一版不穩定 workflow 已主動撤回，避免主線保留不穩實作：

```text
7f99619c...  first workflow
760edcf57b61f27a74595e3f32d2f2b2c4e9f74b  remove unstable workflow
```

穩定 OpenWorker source ingress workflow：

```text
.github/workflows/engineering-source-ingress-win11.yml
```

但後續 REAL evidence 顯示 OpenWorker repo 自身沒有可靠 self-hosted success history，因此 Case 0004 的正式 execution governance 改放 AI-Engineering-OS；OS workflow checkout OpenWorker operator code，產品/專案 ownership 不變。

---

## 4. 為何先做 O87 exact source locator

DWG_todo 歷史 repo 已存在：

```text
output/S1-1140926/
```

且歷史 README 記錄曾在：

```text
D:\AI-Civil\DWG_todo
```

重做真實 S1-1140926 工程案例。

因此正式策略不是立刻重新傳 1.4 MB，而是先讓 **O87 Action 自己找是否仍保存同一份 source bytes**；但只有本次上傳檔案的：

```text
size + SHA256 + AC1032 header
```

全部一致才可接受。

不能因檔名相同就當同一來源。

---

## 5. Source Locator 工具

OpenWorker 新增：

```text
scripts/engineering_source_locator.py
```

### 第一版

Commit：

```text
264578a2907265946e047bf159aa335a5cee96a9
```

支援受限 exact paths / non-recursive glob。

OpenWorker repo 自己的 workflow：

```text
.github/workflows/engineering-source-locator-win11.yml
```

Commit：

```text
2ad5c47caabe147f64cc49436aaa93709227afb9
```

但 OpenWorker workflow run 一直 queued；同 repo 既有 H11 / Case0002 / Case0003 self-hosted workflows 也沒有成功紀錄，因此不能把 OpenWorker repo 本身當可靠 execution scope。

### execution governance 修正

改由 private AI-Engineering-OS 正式 workflow 執行 OpenWorker operator：

```text
liuxb99/AI-Engineering-OS
.github/workflows/case-0004-source-locator-o87.yml
```

Commit：

```text
691daf50a86ab85bfb15ea3f3ccb69ae53766844
```

它只負責 Action transport / host gate；locator 邏輯仍在 OpenWorker。

---

## 6. REAL Locator Attempt 1

Request commit：

```text
43a5908d52650baecb83f5b3c63f02f85eedaeca
```

Formal Action：

```text
Repository: liuxb99/AI-Engineering-OS
Workflow: Case 0004 Source Locator O87
Run: 31932467134
Job: 95129283871
Runner: DESKTOP-O87PJNR-R001
Machine: DESKTOP-O87PJNR
```

### O87 gate

```text
PASS
```

### 查找 contract

6 個受限候選位置，包括：

```text
D:\AI-Civil\DWG_todo\output\S1-1140926\source\...
D:\AI-Civil\DWG_todo\...
D:\AI-Work\jobs\0004-DWG-TO-3D\input\...
D:\AI-Example\0004\...
```

### 結果

```text
SOURCE_LOCATOR_NO_MATCH
candidate_count = 6
physical_checked_count = 0
matched = false
```

結論：不是找到同名檔但 SHA 不同；是這些 exact paths 根本沒有 physical file。

狀態：

```text
FAIL / VALID REAL EVIDENCE
```

不能算 P0 成功。

---

## 7. 缺口 1：只知道 exact path 不夠

Attempt 1 暴露 locator 缺口：歷史 source 可能搬家，因此需要 bounded recursive discovery。

修復 owning repo：

```text
liuxb99/openworker
scripts/engineering_source_locator.py
```

Commit：

```text
8dfb86b914aa443a2684de29acd32330c060490d
```

新增：

- search_roots；
- name_patterns；
- max_depth；
- bounded candidates；
- 排除 `.git / node_modules / venv / __pycache__ / _work`；
- size 不符就不算 SHA，減少無謂 I/O。

Request commit：

```text
d60593d50c7071e5e3c2a409673055565c39c4d9
```

---

## 8. REAL Locator Attempt 2

Formal Action：

```text
Run: 31932529653
Job: 95129433303
Runner: DESKTOP-O87PJNR-R001
OpenWorker operator commit: 8dfb86b914aa443a2684de29acd32330c060490d
OS request head: d60593d50c7071e5e3c2a409673055565c39c4d9
```

O87 hostname gate：

```text
PASS
```

Bounded search roots：

```text
D:\AI-Civil      exists=true
D:\AI-Work       exists=true
D:\AIWork        exists=true
D:\AI            exists=true
D:\PyWork        exists=false
E:\AI            exists=false
E:\AIWork        exists=false
```

Patterns：

```text
S1-1140926*.dwg
*1140926*.dwg
```

結果：

```text
所有存在 search root 的 name_match_count = 0
physical_checked_count = 0
matched = false
SOURCE_LOCATOR_NO_MATCH
```

結論：O87 工程 roots 內沒有以 S1-1140926 命名的 DWG。

狀態：

```text
FAIL / VALID REAL EVIDENCE
```

---

## 9. 缺口 2：歷史 DWG 可能改名

Attempt 2 仍不能證明 source bytes 不在 O87，因為檔案可能改名。

因此補第三版 locator：

```text
*.dwg bounded recursive scan
→ 只 stat size
→ 只有 size == 1,385,583 bytes 才列為 hash candidate
→ 只有 exact-size candidate 才算 SHA256/header
```

這避免：

- 對整個工程磁碟所有 DWG 計 SHA；
- 一堆不相關 DWG 消耗 candidate cap；
- 無界 filesystem 掃描。

OpenWorker repair commit：

```text
a062c67e54e8c57b1d8299409aae5ee328f132bb
```

OS Attempt 3 request commit：

```text
a6a3bca6029906a7f0dd2563497230e1363ac42d
```

Search roots 縮為已確認存在的工程 roots：

```text
D:\AI-Civil
D:\AI-Work
D:\AIWork
D:\AI
```

Pattern：

```text
*.dwg
```

驗收仍完全不變：

```text
size = 1,385,583
SHA256 = aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6
header starts AC1032
```

任何只符合檔名或 size 的文件都不能被接受。

---

## 10. 當前 blocker / next action

目前狀態：

```text
P0 SOURCE LOCATOR ATTEMPT 3 TRIGGERED
O87 ROUTING VERIFIED
OS/OpenWorker INGEST PRODUCTION CODE IMPLEMENTED
PRIMARY SOURCE NOT YET MATERIALIZED
```

下一步：

```text
1. 等 Attempt 3 terminal。
2. 若 exact SHA match：直接進正式 source ingress，建立 OS Project/Job + JobBinding + input/source.dwg + source-dwg Artifact。
3. 若 Attempt 3 仍無 match：正式判定 SOURCE_BYTES_NOT_PRESENT_ON_O87。
4. 不要求使用者手工複製；改走 private immutable source transport。
5. Transport 完成後同一 P0 ingress contract 重跑，成功才進 P2 real DWG overview / visual-search。
```

P0 不會因為 locator 工具本身可執行就標 `PASS`；只有 canonical `input/source.dwg` 在 O87 上通過原始 SHA/size/header，並完成 OS/OpenWorker governance，才算 P0 CLOSED。
