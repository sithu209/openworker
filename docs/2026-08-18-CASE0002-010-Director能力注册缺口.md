# CASE 0002 / 010 Director 能力注册缺口

## 发现

2026-08-18 governed-v2 在 DESKTOP-ODAQN0D 上读取本机 durable Worklist 后，确认 canonical step 仍为 `0002-010`，允许动作是 `comfyx-studio.director.preproduction`。

随后 go-tool `/agent/query` fail-closed：能力目录没有该 action，因此不能启动业务执行。这个失败是正确的治理行为，不应绕过 Worklist，也不应直接复用会继续出图的旧 `operator-0002-storyboard-image-real.yml`。

## 修复

Comfyx-Studio 新增：

- `.github/workflows/operator-0002-director-preproduction.yml`
- 只执行 fresh structured Director preproduction
- 使用 OpenWorker persisted `workspace_root` / `assigned_host`
- COMPUTERNAME fail-closed routing
- 验证 structured characters / scene_bibles / shots
- 生成 `director-preproduction-receipt.json`
- 完成 preproduction 后立即结束，不生成 storyboard image / video / presentation

对应提交：`01ac9625ec2d55b0be567c40d9f5f85781b15f70`

go-tool-runtime 新增 capability：

- `comfyx-studio.director.preproduction`
- owning repo: `liuxb99/Comfyx-Studio`
- workflow: `operator-0002-director-preproduction.yml`
- required inputs: `workspace_root`, `assigned_host`

对应提交：`37bfde802655124d5d7cd1a3962f89d9090854ec`

## 验收

不能只以 capability 文件存在为成功。必须由 governed-v2 重新读取本机 Worklist，并得到：

1. `/agent/query` 暴露 `comfyx-studio.director.preproduction`；
2. Action 在 OpenWorker assigned host 上执行；
3. receipt 含 `run_id / director_plan / director_plan_sha256 / shot_count / character_count / scene_bible_count`；
4. OpenWorker 接受 evidence，010 PASS；
5. canonical next 才能进入 020。

在以上 REAL evidence 出现前，本缺口状态为 `IMPLEMENTED / WAITING FOR LIVE VERIFICATION`。
