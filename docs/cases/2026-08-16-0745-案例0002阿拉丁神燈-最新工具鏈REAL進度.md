# 案例 0002 阿拉丁神燈：最新工具鏈 REAL 閉環進度

更新時間：2026-08-16 11:02 Asia/Taipei

狀態：`IMPLEMENTING / SHOT-1 REAL PROVENANCE CLOSED / VISUAL QC NEXT`

## 開發原則

案例 0002 的目的不是重現舊版工具，而是用真實案例持續驗證所有工具的最新正式提交。任何最新工具暴露出的缺口都直接修 owning repo，再用同一路徑重跑；SHA 只作 provenance，不作舊版 pin。

## 本批新發現與修復

1. 舊 Studio service 污染 REAL run。
   - Run `31922357447` 已真實生成 MP4，但 Studio 回傳 `comfyui:<prompt_id>` 而不是 audited `cx_...`。
   - 根因不是 audited wrapper 或 parser，而是 `127.0.0.1:18082` 已被前一個 0002 Studio process 佔用；新 Studio bind 失敗後，driver 誤連舊服務。
   - 新增 Windows ownership guard，只允許清理已知 0002 run root 下的 `comfyx-studio-platform.exe`，非 0002 listener 一律 fail-closed。

2. ComfyX Intelligence 也存在同型 stale service lifecycle 缺口。
   - Run `31922826238` 成功啟動本 run Studio，但 Director 呼叫 `127.0.0.1:8319` 時 connection refused。
   - ownership guard 擴展到 `comfyx-intelligence.exe:8319`，本 run 不再盲目重用殘留 Intelligence process。

3. audited execution contract 永久測試加強。
   - production path 若再次退回 `comfyui:<prompt_id>`，測試直接 fail。
   - generation trace `ExecutionID` 必須與 audited submission 一致。
   - `NewComfyXCLIInvoker("")` 預設必須是 `comfyx-audited-tool`。

## Shot 1 REAL 閉環證據

有效 REAL run：`31923007861`，attempt `5`，runner `DESKTOP-ODAQN0D-R018`。

實際使用 ComfyX 最新 HEAD：`d49e89fcbcb2213704207d179281ab4be7a324ca`。

完整鏈：

`latest ComfyX snapshot → latest raw/audited tool build → fresh ComfyX Intelligence → fresh Studio Director → ProductionQueue → audited ComfyX → H3 REAL → execution ledger → Studio canonical MP4 → SHA256 identity`

結果：**PASS**。

- queue：`case-0002-single-shot-production` → `succeeded`
- Shot：`shot.generate:shot-1`
- acceleration profile：`lightx2v-h3-4step`
- generation canvas：`1280x736`
- expected duration：`5s`
- H3 REAL generation duration：`268011 ms`（約 4 分 28 秒）
- execution ID：`cx_51992cb4d86c38d3d2025fb1`
- prompt ID：`638f1014-8311-4828-9ca3-fe55b991b78d`
- ComfyUI MP4：`MiniMax_H3_00021_.mp4`
- MP4 size：`1,099,259 bytes`
- SHA256：`c0ee0be6d91be78968253e5a57108ea7fcb7ef544f6fc6a1784eceebd1996f1d`
- Studio canonical path：`D:\AI-Example\0002\runs\31923007861\workspace\case-0002-single-shot\production\shots\shot-1\video.mp4`
- source / execution-ledger / canonical workspace SHA256：**三方一致 PASS**
- final H3 prompt 保留 `Aladdin` 與 `magic lamp` 故事語意：PASS

## 目前已完成

- 原始阿拉丁故事輸入：PASS。
- duration-driven 5 秒 Shot 1 plan：PASS。
- fresh ComfyX Intelligence lifecycle：PASS。
- fresh Studio service ownership：PASS。
- Director / ProductionQueue → audited ComfyX：PASS。
- audited `cx_...` execution ledger：PASS。
- H3 REAL 5 秒 MP4：PASS。
- physical provenance：PASS。
- Studio canonical workspace materialization：PASS。
- source / ledger / canonical SHA256 identity：PASS。
- 最新工具政策：PASS。

Shot 1 現在已完成**機器可驗證的 REAL provenance closure**，但尚未宣告視覺 ACCEPT。

## 下一個驗收點

立即對 Run `31923007861` / execution `cx_51992cb4d86c38d3d2025fb1` / SHA256 `c0ee0be6d91be78968253e5a57108ea7fcb7ef544f6fc6a1784eceebd1996f1d` 進行 Shot 1 Visual Semantic QC。

Visual QC 必須確認代表影格確實呈現阿拉丁／古老黃銅神燈／沙漠古城等目標敘事，而不是只確認 MP4 可播放。

視覺 PASS 後：

`Shot 1 ACCEPT → 不再重跑 Shot 1 → Shot 2–4 REAL → 1280×720 Final Assembly → 字幕 / QC → OS Artifact Registry → Delivery Revision → delivery/website/index.html`
