# Case 0004：新版本機總控遷移實作進度

- 更新：2026-08-18 15:24（Asia/Taipei）
- 固定機：`DESKTOP-O87PJNR`
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 主手冊：`docs/2026-08-18-1412-Case0004-本機總控並行Action遷移與執行手冊.md`
- Drive P0 手冊：`docs/2026-08-18-1505-Google-Drive-Local-Transport-CLI-P0缺口與實作手冊.md`

## 新版基準

Case 0004 已正式採用：

```text
OpenWorker = 本機總控 / durable state / queue / slots / process / progress
go-tool    = 工具 capability / entrypoint / constraints / negative knowledge
GitHub     = source + docs + CI + fallback，不是 business scheduler
Google Drive = ChatGPT review exchange，不是 WorkLedger durable authority
```

後續任何 Case0004 操作若仍以 GitHub workflow queue 作 canonical blocker，視為走回舊架構。

## 已完成的執行層遷移

1. `DWG_todo/scripts/operator_dwg_story_index_local.ps1`
   - 純本機 Story Index operator；
   - 不呼叫 `gh workflow run`；
   - 直接 build / 啟動 CAD service；
   - 直接呼叫 `cad.build_story_index` / `cad.render_story_viewports`；
   - receipt 帶 OpenWorker job / slot provenance；
   - commit：`6f72a76a18036e74912c9449241502e7449f63f7`。

2. `openworker/scripts/case0004_submit_story_index_local.ps1`
   - 查 OpenWorker node / cluster agents；
   - 問 go-tool capability；
   - fail-closed；
   - POST durable local job；
   - GitHub 不持有長任務；
   - commit：`491611bd8b3c6c4e7b5efe17bad0ca1cc7a2a87e`。

3. go-tool Story Index capability 已改 local-first。
   - GitHub workflow 僅 legacy / emergency；
   - commit：`8ea4226dcf275aeb95a461d5b89b6e1e84802926`。

4. go-tool queue-admin 已改 OpenWorker durable queue authority 優先。
   - 清隊列為本機即時 operation；
   - commit：`99bed66f22137658f160c8858f1173b2c57a92dd`。

5. Case0004 Worklist revision 5。
   - `parallel_policy.authority = openworker-local-supervisor`
   - `max_local_slots = 4`
   - `github_business_scheduler = false`
   - `0004-047 = fanout`
   - `0004-049 = join`
   - commit：`cd9a61e8a75f9757913664b957e813ad33bc2abd`。

6. 主手冊已把新版／舊版差異、Action 語義、queue/drain、並行模型、Supervisor 規則寫清楚。
   - commit：`49198d3c349bccff73d83758633bbd75a8af05a4`。

7. Google Drive Local Transport P0 已實作第一批：
   - `coworker/google_drive_transport.py`
   - `coworker/google_drive_cli.py`
   - `openworker-drive` entrypoint
   - 支援 OAuth refresh / authorized_user credentials
   - 支援 `auth-check / upload / list / download / review-publish`
   - atomic receipt / SHA / size 驗證
   - go-tool 已登記 `drive.auth.check / drive.file.upload / drive.file.list / drive.file.download / drive.review.publish`
   - P0 狀態：`IMPLEMENTED — WAITING FOR REAL O87 VERIFICATION`。

8. Case 0004 overview publisher 已改成共用 Drive CLI：
   - `scripts/case0004_publish_overview_drive.ps1`
   - 只負責固定 O87、source、SHA authority 驗證與 Case receipt；
   - 真正 Google Drive transport 交給 `openworker-drive`；
   - 不再自行組 multipart / 不再要求案例腳本持有短效 access token；
   - commit：`e05ddc6dbe5ae9a9046a8fa6dc3b17b7bdd9010e`。

9. 新增 Case 0004 本機 durable Drive submitter：
   - `scripts/case0004_submit_overview_drive_local.ps1`
   - 固定 `DESKTOP-O87PJNR`；
   - 先驗證 overview authority SHA；
   - 先查 OpenWorker node / agents；
   - 先問 go-tool `drive.file.upload / drive.review.publish`；
   - 再 POST OpenWorker durable job；
   - 使用 resource locks：`transport:google-drive`、`workspace:case0004-review-handoff`；
   - submit receipt：`.openworker/evidence/latest-drive-local-submit.json`；
   - business upload receipt：`receipts/case0004-overview-drive-handoff.json`；
   - commit：`e678537fbd471eca428581064b1230baf388d3e5`。

## REAL Case durable 狀態

```text
0004-010 PASSED
0004-020 PASSED
0004-030 PASSED
0004-040 PASSED
0004-045 READY
```

Authority inputs：

- DWG SHA256：`aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6`
- overview：`D:\AI-Work\jobs\0004-DWG-TO-3D\dwg\exports\default\visual-search\case0004-overview.png`
- overview SHA256：`5cee03340cbbcad51e412b46b85bda9dcaac22b193586b953bbfd5134039103e`
- canonical next step：`0004-045 cad.build_story_index`

禁止重跑 `0004-010 ~ 0004-040`。

## Story Index REAL contract

程式端已確認 `cad.build_story_index` fail-closed 驗證：

```text
camera_bounds: min_x/min_y/max_x/max_y
image_width >= 64
image_height >= 64
stories[] 非空
stories[].story_id 必填且唯一
stories[].pixel_bounds = [min_x,min_y,max_x,max_y] 且必須在影像內
stories[].confidence = 0..1
stories[].visual_basis 可選
```

因此不能猜樓層框。

## REAL overview evidence handoff 狀態

2026-08-18 本輪重新檢查：

- Google Drive 搜尋 `case0004`：仍無結果；
- `DWG_todo/review-tmp/case0004/latest-drive-visual-handoff.json`：仍不存在；
- `DWG_todo/review-tmp/case0004/latest-overview-artifact-handoff.json`：仍不存在。

所以舊 emergency transport 沒有可用 durable evidence；不得把它標成成功。

新版 canonical local path 已完整到「可提交」：

```text
case0004_submit_overview_drive_local.ps1
  -> go-tool capability check
  -> OpenWorker POST /v1/jobs
  -> case0004_publish_overview_drive.ps1
  -> openworker-drive upload
  -> Google Drive
  -> receipts/case0004-overview-drive-handoff.json
```

目前聊天工具面仍無法直接呼叫 O87 的 `127.0.0.1:8787 / 8848`，因此本輪沒有偽造 local durable ack、沒有偽造 OAuth 成功、沒有偽造 Drive receipt。

## 現在真正要做的工作

```text
O87 執行 local durable Drive publish
-> receipt 顯示真實 drive_file_id
-> ChatGPT Drive connector 能找到 case0004-overview.png
-> ChatGPT visual localization
-> case0004-story-index-params.json
-> OpenWorker local durable submit 0004-045
-> REAL Story Index artifact
-> 0004-047 per-story fan-out（最多 4 slots）
-> 0004-049 join + multimodal review
```

## 新版驗收觀念

### Control path 通

OpenWorker 接受 action / job、分配 durable identity，代表本機 control path 已通，不必像舊 GitHub Actions 一樣阻塞等待長任務。

### Business step 完成

仍必須看到真實 artifact、SHA256、receipt、Worklist acceptance 才能標 PASSED。

### Drive transport 完成

必須至少同時成立：

- `openworker-drive` 真實 upload 成功；
- receipt 有 `drive_file_id` + source SHA；
- Google Drive connector 可重新發現該檔；
- ChatGPT 可讀實體 PNG。

只看到 submit ack，不能標 Drive handoff 成功。

## 當前阻塞判定

不是：

- CAD overview 未生成；
- `0004-010~040` 要重跑；
- Story Index 工具不存在；
- Drive CLI 還沒寫；
- 必須恢復 GitHub business queue。

而是：

**O87 尚未產生新版 local durable Drive publish 的真實 ack / business receipt，因此 ChatGPT 仍看不到 REAL overview。**

## 下一步

1. O87 執行 `scripts/case0004_submit_overview_drive_local.ps1`；
2. OpenWorker 接案後即代表 control path 可用，不必等 GitHub；
3. 查 `.openworker/evidence/latest-drive-local-submit.json` 與 `receipts/case0004-overview-drive-handoff.json`；
4. Drive connector 一旦能發現 overview，立即 visual localization；
5. 形成 `case0004-story-index-params.json`；
6. `0004-045` 走 `case0004_submit_story_index_local.ps1`；
7. Story Index 完成後 fan-out `0004-047`。

## 狀態

**CASE0004 0004-045 READY — LOCAL DRIVE DURABLE PATH IMPLEMENTED — WAITING FOR REAL O87 SUBMIT/UPLOAD RECEIPT**
