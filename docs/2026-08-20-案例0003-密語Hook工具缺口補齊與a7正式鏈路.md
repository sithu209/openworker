# 案例0003 — 密語 Hook 工具缺口補齊與 a7 正式鏈路

- 日期：2026-08-20（Asia/Taipei）
- Case：0003 / 玉井橋 / YUJING BRIDGE
- 固定 business machine：DESKTOP-UL7V2VV
- Workspace：D:\AI-Work\jobs\0003-YUJING-BRIDGE
- 正式密語：CASE0003.ORTHOPHOTO.CONTINUE

## 正式架構

`ChatGPT / LLM → GitHub Action（只發密語）→ DirectWork Hook → DirectWork 總控解析 allowlist phrase → durable work → queue → slot/executor → UL7 → artifact/evidence`

GitHub Actions 不是 business authority，也不得在正式案例鏈路自行 inline business command。DirectWork 才負責把 phrase 轉為 durable work。

## 已確認的工具缺口

檢查 DirectWork 真實 source 後，原 `internal/api/server.go` 沒有任何 Hook route，只有 `/v1/work` 等 work API；因此過去 Case0003 secret workflow 本質上是 Action 自己組 business command 再 POST `/v1/work`，與正式架構不符。

定義：`GAP-0003-HOOK-01 — DirectWork 缺少正式密語 Hook / phrase registry。`

## 已補工具 source

已安全進 DirectWork master：`af172d5398122fe8f90b1b1885bf2ace6a221777`

內容：

- `POST /v1/hook/secret`
- Hook request 只含 schema/phrase/request_id
- 本機 `hooks.json` allowlist registry 決定真正 command/cwd/workspace/machine
- unknown phrase → 404
- registry unavailable → 503
- cwd unavailable → 409
- command 仍經 runtime ValidateCommand
- `Store.FindByDispatchID`：相同 request_id durable idempotency，不重複開 work
- UL7 registry 註冊 `CASE0003.ORTHOPHOTO.CONTINUE`
- DirectWork-owned handler：`hooks/case0003-orthophoto.ps1`
- handler stage：workspace / geolocation / tool-discovery / test-build / acquire / acceptance / succeeded
- 結果固定落 `D:\AI-Work\jobs\0003-YUJING-BRIDGE\evidence\case0003-orthophoto-hook-result.json`
- deploy-and-smoke 已補 hooks.json / hooks/* 部署，並用 unknown phrase 404 驗 Hook route
- API 中文文檔已補正式 Hook contract

## a6 必須降級為診斷旁證

`case0003-orthophoto-20260820-a6`：

- work_id=dw-20260820T081042-75c0561d89e27853
- accepted PASS
- claimed slot=3
- running pid=22184
- failed exit=1

它是 O87 直接 POST `/v1/work` 到 UL7，證明 O87→UL7 LAN、UL7 DirectWork durable admission、queue、slot/executor 能工作；但繞過 Hook，所以不算正式密語閉環。

## UL7 fresh infrastructure evidence

已取得：

- DNS 100.78.110.79
- `/v1/node/status` HTTP 200 / online=true
- fresh probe 時 free_workers=4 / busy=0 / queued=0
- ODA / O87 peers online
- DirectWork repo runner service `actions.runner.liuxb99-DirectWork.DESKTOP-UL7V2VV-R001` Running / Auto
- service PID 18052
- `.runner` agentName=DESKTOP-UL7V2VV-R001，repo binding=liuxb99/DirectWork
- install labels=ai-ci, ai-server, UL7
- Runner.Listener.exe active PID 25012

所以不可再說 UL7 主機或 DirectWork offline，也不應重裝所有 runner。觀察到 runner log 曾有 job cancellation / broker transport I/O error，且同時間其他 Case0006 仍會占用 DirectWork repo UL7 runner，因此較像單 repo runner queue/session contention。

## UL7 Hook cutover

Hook source 合入後，已更新：

`deploy/ul7-cutover-trigger.txt`

trigger commit：`ba2757a4711c5b426c25d551d50b57969bed0a8a`

content：`case0003-secret-hook-cutover-20260820-a7`

既有 `.github/workflows/deploy-ul7-only.yml` 應執行：go test/build → deploy → durable smoke → Hook route smoke → verification receipt。

截至本同步點，尚未看到 fresh deploy receipt，因此只能寫：

`CUTOVER TRIGGERED / FRESH HOOK DEPLOY NOT YET PROVEN`

不得提前宣告 Hook 已在線。

## 正式 transport-only workflow

已補並安全進 DirectWork master：`42fa25b722037bdf1a00fee0eb5785e48c4a580b`

workflow：`.github/workflows/secret-case0003-hook-o87-to-ul7.yml`

它固定由 fresh healthy O87 作短命令 transport，target：

`http://DESKTOP-UL7V2VV:8787/v1/hook/secret`

只 POST：

```json
{
  "schema":"directwork.secret-command.v1",
  "phrase":"CASE0003.ORTHOPHOTO.CONTINUE",
  "request_id":"case0003-orthophoto-20260820-a7"
}
```

Action 不包含 business command。Hook 回 work_id 後，Action 只做 observation：poll work/events/artifacts/result，將 receipt 寫回 GitHub；receipt 明記 `github_action_used_for_business_execution=false`。

目前刻意尚未送 a7 request，原因是 UL7 deploy receipt 尚未證明 Hook binary 已運行。這不是停工，而是避免把正式密語打到舊版 8787 造成假 failure。

## 最新 acceptance matrix

```text
DirectWork Hook source master          PASS af172d53...
Hook allowlist/idempotency             PASS source
Case0003 Hook handler                  PASS source
Hook deploy support                    PASS source
UL7 cutover trigger                    SENT ba2757a...
Fresh UL7 Hook running binary          NOT YET PROVEN
Fresh UL7 deploy receipt               NOT YET OBSERVED
Formal transport-only workflow         PASS source 42fa25b7...
Formal a7 request                      NOT SENT BY DESIGN
Formal a7 Hook work_id                 PENDING
Fresh PHOTO2 JPEG                      NOT PROVEN
JPEG size/SHA256                       NOT PROVEN
NLSC PHOTO2 acceptance                 NOT PROVEN
Drive verified publish                 NOT STARTED
ChatGPT exact-image visual review      NOT STARTED
```

## 下一個 gate

1. 先取得 fresh UL7 cutover receipt，確認 running commit + durable smoke + `hook_probe=route-pass-unknown-phrase-404`。
2. 建立正式 a7 request。
3. GitHub Action 只把 a7 密語發給 Hook。
4. DirectWork Hook 必須回 fresh work_id；接著追 accepted/claimed/running/terminal。
5. 若 failed，直接讀 hook result 的 stage/error 補最小 gap。
6. 若 succeeded，驗 JPEG path/size/SHA256、provider=nlsc、layer=PHOTO2、tile_count<=25。
7. orthophoto durable PASS 後才進 Drive publish；ChatGPT 必須拿到 exact JPEG 並實際看圖，才能把 visual QA 記為 PASS/TUNE/FAIL。
