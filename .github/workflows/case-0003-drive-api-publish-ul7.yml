name: '[RETIRED] Case 0003 Google Drive API Review Publish UL7'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired business execution path
        shell: bash
        run: |
          echo 'CASE0003_DRIVE_API_WORKFLOW_RETIRED'
          echo 'Canonical path is OpenWorker local controller v7 -> Drive Desktop sync immutable ZIP -> ChatGPT Google Drive connector review.'
          echo 'This workflow is retained only as historical migration evidence and must not publish business artifacts.'
          exit 1
