# Case 0004 — Story Region 到柱候選執行節點

時間：2026-08-18 10:10（Asia/Taipei）

本文件補充 Case 0004 逐步執行日誌，記錄 `R-0004-050-002 → 0004-050 → 0004-055` 這一個具體節點的 REAL 狀態、發現的 contract gap 與修復。

## 1. Fresh O87 durable state

為避免根據舊 receipt 猜測進度，重新觸發 `case-0004-o87-worklist-reconcile.yml`。

觸發 commit：`8356a20089c2df2475e4e49cb0520c26f0a78a1d`

REAL state 回寫 commit：`a8d1405207eb71a32d9f640c622a1f1e8ee30f37`

REAL state：

- run：`32090849398`
- machine：`DESKTOP-O87PJNR`
- runner：`DESKTOP-O87PJNR-R004`
- durable revision：`130`
- canonical next：`R-0004-050-002`
- `0004-050`：`BLOCKED`
- `R-0004-050-002`：`RUNNING`

結論：舊 `case-0004-o87-apply-story-region-review.yml` 沒有成功改動 durable Worklist；不能宣稱 Story Region 已 promotion。

## 2. `0004-055` acceptance contract 重新核對

最新版 `case-worklists/0004.json`：

```text
0004-055 acceptance =
source_sha256
story_region
candidate_count
confirmed_column_handles
review_decision
```

這表示 055 包含兩個不可混淆的階段：

1. 工具階段：列出候選。
2. ChatGPT 視覺階段：確認真正柱 handles 並作 review decision。

舊 workflow 在工具列完候選後就呼叫 `accept_action_evidence()`，只提供 `column_candidates / candidate_count / review_required`；這不符合 canonical acceptance，必須 fail-closed。

## 3. 修復

新增兩階段 workflow：

`.github/workflows/case-0004-o87-story-region-to-column-review.yml`

commit：`955f44dbfef7e1edff9df93b1f0c24bb37951588`

修復後行為：

```text
R-0004-050-002 ChatGPT receipt
→ repair PASS
→ parent 0004-050 自動解鎖
→ REAL cad.set_story_region(1F)
→ 0004-050 acceptance PASS
→ REAL cad.list_story_column_candidates
→ 記錄 source_sha256 / story_region / candidate_count / candidate artifact
→ complete_action only
→ 0004-055 保持 RUNNING
→ ChatGPT 實際檢查候選
→ confirmed_column_handles + review_decision
→ PASS 0004-055
```

這次刻意不讓工具 workflow 自動批准柱，保持 column authority fail-closed。

## 4. 當前成果狀態

截至此節點：

- `story-region-probe.json`：REAL PASS，73 entities。
- ChatGPT Story Region receipt：已存在，決策 `PROMOTE_1F_STORY_REGION`。
- fresh Worklist：仍在 `R-0004-050-002`。
- `story-region-column-candidates.json`：尚未回寫。
- `column-candidates-1f.json`：尚未回寫。

因此不得宣告 `0004-050`、`0004-055` 已 PASSED。

## 5. 下一個 acceptance

下一次只接受實體 evidence：

1. Worklist repair 變 `PASSED`。
2. `0004-050` 變 `PASSED` 且 1F Story Region persisted。
3. `column-candidates-1f.json` 實體存在且 candidate_count >= 2。
4. ChatGPT 查看候選後，才寫 `confirmed_column_handles` 與 `review_decision`。
5. 只有 055 PASS 才准進 `0004-060 cad.design_story_structure`。
