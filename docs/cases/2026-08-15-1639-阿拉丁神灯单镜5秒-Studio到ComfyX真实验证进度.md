# 阿拉丁神灯单镜 5 秒：ComfyX-Studio → ComfyX REAL 验证进度

更新时间：2026-08-16 07:35（Asia/Taipei）

状态：IMPLEMENTING / SHOT-1 TECHNICAL + PROMPT SEMANTIC GATES GREEN / STUDIO WORKSPACE REAL GATE RUNNING

## 当前目标

Case 0002 先闭环一个 5 秒 REAL shot：

```text
GitHub self-hosted Action
→ Studio Director
→ ProductionQueue
→ audited ComfyX executor
→ execution ledger
→ MiniMax H3 lightx2v-h3-4step
→ fresh MP4
→ ComfyX physical provenance
→ Studio canonical workspace MP4
→ hash-equivalent workspace provenance
→ visual semantic QC
```

旧 `MiniMax_H3_00006_.mp4` 永久排除，不作为成功证据。

## 已完成基础修复

- H3 generation canvas：`1280x736`；future delivery：`1280x720`。
- ComfyX durable execution ledger 已记录 execution/job/shot/prompt/path/size/mtime/SHA256。
- Studio 已使用 audited ComfyX wrapper。
- ComfyX pinned commit：`43a91f7962914cbba14a050a7af6a92e0d326705`。
- `lightx2v-h3-4step`：4 steps + fresh crypto seed。
- ComfyX source 使用 run-local isolated snapshot，同时 build raw/audited tools。
- 关键历史修复 commits：`6bf2758`、`7246d16`、`8a4e2d0`。

## Attempt 6：技术链 PASS，但语意 REJECT

旧 REAL Run：`31878632235` / Job `94998732438`。

该次生成 fresh `MiniMax_H3_00001_.mp4`，execution/provenance 均正确，但 execution ledger 的 final H3 prompt 只有 camera/composition，没有阿拉丁故事，因此正式标记：

```text
TECHNICAL PASS
SEMANTIC REJECT
NOT ACCEPTED AS SHOT-1
```

这次缺口促成后续 narrative propagation 修复。

## narrative propagation 根因与修复

真正 owning gap：

```text
DirectorProjectPlan.Shot.NarrativePurpose
→ ProductionQueue.input[narrative_purpose]
→ AIGCDirectorProductionQueueExecutor
→ ShotDirectionIR.NarrativeContext
→ AIGCSemanticCompiler
→ final H3 prompt
```

`AIGCSemanticCompiler` 原本已经支持 `NarrativeContext`；丢失点是 queue executor 没把 queue `narrative_purpose` 合并进 canonical `ShotDirectionIR.NarrativeContext`。

修复 commit：`c9f22dbf886cbd9e65dfada994461816fff33f79`。

永久测试 commit：`59c0138a3ebb1eaf347d5f7d02d1ed744930ad9f`。

## REAL workflow 语意 fail-closed

Commit：`15021445c9bc13c880be99cb3b39ea8c40dc2a68`。

Case 0002 专用 REAL workflow 会直接读取 ComfyX execution ledger 的 `arguments.prompt`；若 final H3 prompt 缺 `Aladdin` 或 `magic lamp`，Action 直接 FAIL。业务代码保持 provider-neutral。

## AIGC Domain Win11 Gate：全绿

Run：`31879359105` / Job：`94999661459`。

```text
checkout PASS
runner/Win11 PASS
download modules PASS
format PASS
targeted tests PASS
go vet PASS
full Go tests PASS
race PASS
upload diagnostics PASS
production build PASS
```

## 最新已完成 REAL：技术 + prompt 语意链全绿

Run：`31879397040`

Job：`94999747022`

Runner：`DESKTOP-ODAQN0D-R018` / Machine：`DESKTOP-ODAQN0D`

完整 production path 已 PASS：

```text
Route to ODAQ PASS
Checkout Studio PASS
Snapshot pinned ComfyX PASS
Isolate ComfyUI queue PASS
Prepare 5s Aladdin driver PASS
Build raw + audited ComfyX tools PASS
Drive Studio Director to ComfyX PASS
Verify current-prompt H3 shot PASS
Report evidence PASS
Job SUCCESS
```

### fresh REAL 证据

```text
execution_id = cx_c1e28f11d115321d35e0ba45
prompt_id = 71bc7df9-d395-4a35-bb0b-ad357838c5b0
artifact = MiniMax_H3_00002_.mp4
size = 3,352,746 bytes
sha256 = c3f97db731953322df6168c3ac9c8af5ca4c57ffecc773f393176962b6c262b0
mtime_utc = 2026-08-15T10:28:10.6291552Z
duration_ms = 255347 (~4m15s)
profile = lightx2v-h3-4step
canvas = 1280x736
```

narrative propagation 已由 REAL execution ledger 证明，final H3 prompt 包含 Aladdin / magic lamp 原故事语意。

## 2026-08-16 下一批：Studio 工作目录实体 artifact 闭环

用户指出的真正缺口是：ComfyX/ComfyUI 已经生成 MP4，但 Studio 不能只记录 ComfyX 的绝对路径；Studio 必须拥有自己的 canonical workspace artifact。

此前已完成 contract 层：

- `879bd388844f3c6a9fa833c82a2607250ea41ab4`：加入 `WorkspaceArtifactDestination`，只接受 Studio workspace 相对路径，拒绝 absolute path / `..` escape。
- `9be28b966f1d3d3c055c5b3cead387b9a216e920`：generation trace 可持久化 canonical workspace artifact provenance。
- `13a4df9ac99e85c651d53fb6f36dabed87e29126`：永久测试锁定 destination handoff。

本批继续补 REAL acceptance，不再把 contract test 当作实体闭环。

新 commit：`58616eb20dfbdbbe6c2a31bfe20146e8a89920f1`。

`operator-0002-aladdin-single-shot.yml` 现在在 fresh H3 成功后必须执行：

```text
ComfyX ledger video_full_path
→ Test-Path source MP4
→ Studio run workspace/case-0002-single-shot/production/shots/shot-1/video.mp4
→ Copy-Item materialize canonical MP4
→ non-empty gate
→ source SHA256
→ canonical SHA256
→ source == canonical
→ canonical == ComfyX ledger sha256
→ evidence/06-studio-workspace-artifact.json
```

canonical relative path 固定为：

```text
production/shots/shot-1/video.mp4
```

新的 evidence 会记录：

```text
workspace_id
relative_path
kind
canonical_full_path
source_full_path
execution_id
prompt_id
job_id
shot_id
size
sha256
mtime_utc
materialized_at
```

这使验收从过去的：

```text
ComfyX says succeeded + ComfyUI path exists
```

提升为：

```text
ComfyX fresh physical MP4
+ execution correlation
+ Studio-owned canonical MP4 physically exists
+ byte identity verified by SHA256
```

当前该 workflow 因本次 main push 会自动触发 ODAQ REAL run。只有 run terminal green 且 `06-studio-workspace-artifact.json` 实际产生，才能把 workspace closure 标记为 REAL PASS；目前仍标记 RUNNING/PENDING，不提前宣告成功。

## 性能观察

Attempt 6：`259456 ms`。

narrative-fixed REAL：`255347 ms`。

两次都约 4 分 15～19 秒。慢速不是 narrative 修复造成，后续应独立分析 H3 sampler / VAE decode / save 阶段。

## 最新验收状态

已通过：

1. ODAQ 真正执行 production。
2. target duration = 5 sec。
3. exactly one shot。
4. queue narrative 来自 Case 0002 原故事。
5. narrative 已进入 canonical ShotDirectionIR。
6. final H3 prompt 实际含 Aladdin / magic lamp。
7. audited ComfyX wrapper。
8. lightx2v-h3-4step。
9. generation canvas 1280x736。
10. `cx_` execution_id。
11. prompt correlation 一致。
12. current ComfyUI history 有 fresh MP4。
13. ComfyX physical path / size / mtime / SHA256 完整。
14. Action run/job 可回溯。
15. Studio workspace destination contract 已实现并有永久测试。
16. REAL workflow 已加入 Studio canonical MP4 materialization + SHA256 fail-closed gate。

正在执行/待验收：

17. 新 ODAQ REAL run 必须实际产生 `workspace/case-0002-single-shot/production/shots/shot-1/video.mp4`。
18. `evidence/06-studio-workspace-artifact.json` 必须证明 source/canonical SHA256 一致。
19. 实际观看 canonical MP4 做视觉语意 QC。
20. 对约 255 秒生成耗时做 sampler / VAE decode / save 分段性能诊断。

因此当前状态：

```text
TECHNICAL PASS
PROMPT SEMANTIC PASS
COMFYX PHYSICAL ARTIFACT PASS
STUDIO WORKSPACE CONTRACT PASS
STUDIO WORKSPACE REAL MATERIALIZATION RUNNING
VISUAL QC PENDING
```

## 下一步

第一优先不是再改 contract，而是追本次自动触发的 ODAQ REAL run 到 terminal：确认 `06-studio-workspace-artifact.json` 与 canonical `video.mp4` 实际存在且 hash 相等。

通过后，shot-1 的 artifact ownership 才正式从 ComfyX source provenance 闭环到 Studio workspace；然后直接做 visual QC。visual QC PASS 后立即进入 shot 2–4 → 1280×720 final assembly → 字幕/QC → OS Artifact Registry → Delivery Revision → `delivery/website/index.html`。
