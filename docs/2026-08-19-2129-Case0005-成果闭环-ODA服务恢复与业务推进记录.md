# Case0005 白雪公主：成果闭环 — ODA resident OpenWorker 恢复与业务推进记录

时间：2026-08-19 21:29 +08:00

## 目标

本轮不再以“Embedded Control 控制链可达”作为业务完成标准。Case0005 当前唯一验收目标是：

1. ODA `DESKTOP-ODAQN0D` 的 resident OpenWorker / Local Supervisor 正常运行；
2. Case0005 durable work 被 OpenWorker Case Engine 接受并推进；
3. 本机 go-tool-runtime durable queue / claim / executor 有真实证据；
4. 生成 `0005-025` 文字版 storyboard PPTX 实体文件；
5. 记录 artifact 路径、大小、SHA256、work id、run id、request id；
6. 后续发布 Google Drive，进入 `0005-027` APPROVE/REJECT gate；
7. 未看到实体 artifact 前，不得声明 Case0005 业务完成。

## 本轮已取得 REAL 证据

### 1. ODA queue clear 已真实执行

Request：`20260819-1849-oda-queue-clear-001`

GitHub run：`32241253073`

Runner：`DESKTOP-ODAQN0D-R001`

结果：`accepted=true`、`exit_code=0`。

go-tool-runtime 回报：

- `assigned_host = DESKTOP-ODAQN0D`
- `cleared_pending = 0`
- `cleared_claimed = 0`
- `cleared_total = 0`
- `queued_after = 0`
- `inflight_after = 0`
- `stale_lease_after = 0`

证据文件：

`command-results/oda/20260819-1849-oda-queue-clear-001/final.json`

对应 commit：`24a75fbac3a8561ff337e527cdc9072b16e76380`

结论：GitHub 仅承担 transient command transport；实际 queue clear 由 ODA 本机执行 authority 完成。

### 2. Case status 拒绝的真实原因已定位

Request：`20260819-1806-oda-case0005-status-slots-002`

GitHub run：`32241116156`

Runner：`DESKTOP-ODAQN0D-R001`

结果：`accepted=false`、`exit_code=71`。

真实错误：

`resident OpenWorker unavailable: Get "http://127.0.0.1:8787/v1/node/status": dial tcp 127.0.0.1:8787: connectex: No connection could be made because the target machine actively refused it.`

证据文件：

`command-results/oda/20260819-1806-oda-case0005-status-slots-002/final.json`

对应 commit：`e236c70f533335c4c77e997c3830defa7f1e1a42`

结论：当前阻塞不是 Embedded Control 架构失效，也不是 go-tool-runtime queue 不可达；而是 ODA resident OpenWorker Windows service 当时没有在 `127.0.0.1:8787` 提供服务。

## 本轮修复

### A. 触发完整 ODA resident node 恢复

触发 commit：`1f26f24a997a39ea5aac8eb8dd24cacd2a608675`

message：`ops(case0005): restore ODA resident OpenWorker [bootstrap-oda-node]`

### B. 触发“恢复 resident node + Case0005 Go-native bootstrap”同 job 顺序

触发 commit：`0d480df15d48c42084718332d77b63b080fbe9ac`

message：`ops(case0005): restore ODA node and bootstrap case [bootstrap-oda-node] [case0005-local-bootstrap]`

现有 `.github/workflows/bootstrap-openworker-node-oda.yml` 的职责：

- 固定 `DESKTOP-ODAQN0D`；
- build / install / upgrade `OpenWorkerNode`；
- Workers = 4；
- capability 包含 `case0005`、`local-supervisor`、`storyboard`、`presentation`；
- 验证 `127.0.0.1:8787/v1/node/status`；
- 验证 Go Case continue route；
- 发布 immutable resident deployment receipt；
- 发布 Case0005 当前 durable work receipt；
- 在 `[case0005-local-bootstrap]` 时调用 Go-native Case bootstrap。

### C. 为 Case0005 local kick 增加 resident service 自恢复

修复 commit：`0ebb3168f29e249b071d27c4c03ef60a225252db`

修改：`.github/workflows/case0005-local-controller-kick-oda.yml`

在 Case bootstrap 前新增：

1. `Get-Service OpenWorkerNode`
2. 若不是 Running，则 `Start-Service OpenWorkerNode`
3. 等待 Running
4. 调用 `http://127.0.0.1:8787/v1/node/status`
5. 验证 machine 必须为 `DESKTOP-ODAQN0D`
6. 之后才允许走 `/v1/cluster/route` 和 `/v1/cases/bootstrap`

业务执行 authority 仍是 ODA resident OpenWorker local supervisor，GitHub Action 只负责控制传输和基础设施启动，不承载 Case business execution。

触发 commit：`9000555c94edd3524cc1658bbec73aa056c4602e`

message：`control(case0005): start resident service and kick local controller [case0005-local-kick]`

## 当前边界

截至本记录：

- ODA queue clear：REAL PASS；
- ODA runner 确实执行过 transient command；
- resident OpenWorker `:8787` 掉线原因：REAL 已定位；
- resident service 恢复与 Case0005 kick：已触发；
- 尚未看到新的 `case-evidence/0005/cluster-kick.json` durable ACK；
- 尚未看到新的 `case-evidence/case0005-current-work/latest.json`；
- 尚未取得 `0005-025` 文字版 storyboard PPTX 实体 artifact；
- 因此 **不得声明 Case0005 业务完成**。

## 下一验收动作

只按 durable evidence 继续：

1. 等 ODA 写回 resident deployment / cluster kick receipt；
2. 读取 Case0005 durable work id / stage / state；
3. 如果当前 work 为 pending/claimed/running，只查询，不重复 continue；
4. 如果终态允许推进，发一次 `CASE.CONTINUE_BATCH`；
5. 追到 `0005-025`；
6. 验证 PPTX 实体存在、非空、可打开，并记录 size / SHA256；
7. 发布 Google Drive；
8. 进入用户审批 gate。
