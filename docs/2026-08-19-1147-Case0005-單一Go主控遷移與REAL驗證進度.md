# Case 0005 白雪公主：單一 Go 主控遷移與 REAL 驗證進度

更新時間：2026-08-19 11:47 +08:00

## 1. 案例狀態

- Case ID：`0005`
- 固定機器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- Worklist revision：`13`
- 第一業務步驟：`0005-010 comfyx-studio.director.preproduction`
- 業務 evidence：**尚未取得，仍為 PENDING**

本批工作是 control-plane / Case Engine 遷移，不能把 Go 原始碼完成或 binary 安裝視為 `0005-010` 完成。

## 2. 已決定的新架構

唯一 canonical 主控：`openworker.exe`。

控制鏈：

`ChatGPT / llama.cpp / OpenCode -> openworker.exe -> Go Case Engine -> :8848 durable local queue -> OpenWorker process kernel -> leaf capability`

`openworkerctl.exe` 僅保留 compatibility shim，與 `openworker.exe` 共用 `internal/controlcli`，不得存在第二套邏輯。

Python 僅允許作 leaf capability runtime；Case bootstrap / continue / status / ledger / reconcile / fanout coordinator 不再以 Python controller 作權威。

## 3. 已完成原始碼批次

### G0 統一 CLI

- `internal/controlcli/controlcli.go`
- `cmd/openworker/main.go`
- `cmd/openworkerctl/main.go` 已降為 compatibility shim
- installer 同時建立：
  - `C:\ProgramData\OpenWorker\bin\openworker.exe`
  - `C:\ProgramData\OpenWorker\bin\openworkerctl.exe`

### G1 Go-native bootstrap

- 新增 `internal/casecontroller`
- 直接解析 worklist
- 驗證 Case / machine / workspace / revision / DAG
- materialize `.openworker`
- Go append-only ledger：
  - `go_controller_bootstrap_start`
  - `go_controller_bootstrap_completed`
- 寫入：
  - `.openworker/case-worklist.json`
  - `.openworker/case-controller-last.json`
- bootstrap result：`controller=go-native`、`python_required=false`

### Windows process kernel 修復

ODA REAL test 發現既有 Windows `cmd /S /C` quoting regression：

- PowerShell `exit 7` 曾被誤判 success。
- quoted executable regression test 曾失敗。

已改成 Windows 專用 `SysProcAttr.CmdLine`，保留原始 cmd command line；POSIX 另有獨立 shell builder。

這個修復同時針對先前 Case bootstrap stderr CP950「找不到網路路徑。」所暴露的 process-start 類風險。

## 4. REAL ODA 驗證記錄

### Run 32212483557

第一輪 unified Go build fail-closed：

- `go.sum` 缺 `modernc.org/sqlite`
- `go.sum` 缺 `golang.org/x/sys/windows/registry`

已補 installer `go mod tidy`。

### Run 32212595828

第二輪已成功進入完整 `go test ./...`，暴露：

1. 舊 `cmd/openworkerctl/main_test.go` 仍引用已搬走函式。
2. `case_bootstrap_test.go` 仍使用舊 Python bootstrap fixture。
3. Windows runtime command quoting regression。
4. self-hosted runner 工作區殘留舊 receipt commit，造成 rebase conflict。

以上均已逐項修正：

- CLI tests 移到 `internal/controlcli`。
- API tests 改驗證 Go-native bootstrap、且不建立 Python controller job。
- Windows shell builder 平台化。
- installer receipt 改成先 `fetch + reset --hard origin/main`，再只恢復 immutable receipt/go.sum 後 commit。

### R4

已於 11:45 +08:00 再次觸發 ODA 全套 REAL 驗證。

**目前在本文件更新時尚未取得 R4 terminal install receipt，因此不得宣告 canonical `openworker.exe` REAL_VERIFIED。**

## 5. `go-tool-runtime :8848` 去 Python 缺口

檢查確認舊 `internal/server/openworker_case_handler.go` 仍要求：

- `controller_module`
- `python_exe`

並直接執行 Python controller。

已在 go-tool-runtime 修改：

- 新 request contract 不再包含 Python/controller 字段。
- bootstrap 直接 POST resident OpenWorker `127.0.0.1:8787/v1/cases/bootstrap`。
- `controller=go-native`
- `python_required=false`
- GitHub business execution=false
- Go-native continue 尚未部署時明確 fail-closed：`go_native_continue_not_ready`。
- **禁止 fallback 到任何 `coworker.case0005_*controller`。**

相關中文缺口文檔：

`go-tool-runtime/docs/2026-08-19-1136-OpenWorker單一Go主控-8848CaseDispatch去Python缺口.md`

## 6. 尚未完成：G2

目前只能把 Case bootstrap 主控去 Python；尚未宣告整個 Case Engine 完成。

下一步必須實作：

1. `OpenWorker :8787 POST /v1/cases/continue`
2. Go ready-step scan / dependency gate
3. deterministic execution_id
4. Go 產生 bounded local claim
5. Go 透過 `:8848` 提交固定 capability
6. durable ACK 後寫 ledger / active execution state
7. terminal evidence reconcile
8. acceptance gate 成功才把 `0005-010` 改為完成

第一個目標 action：

`0005-010 -> comfyx-studio.director.preproduction`

輸入至少包含：

- workspace_root
- assigned_host
- case_id
- source_title
- source_story

驗收仍必須完整取得：

- run_id
- director_plan
- director_plan_sha256
- shot_count
- character_count
- scene_bible_count

## 7. 下一個 REAL checkpoint

只有以下全部成立，才可宣告 Case 0005 主控真正切到 Go：

- ODA `openworker.exe` REAL_VERIFIED
- `:8848` 新 Go-native contract 已部署
- `case continue 0005` 不啟動 Python controller
- workspace ledger 出現 Go bootstrap/dispatch event
- `0005-010` 取得真實 durable work_id / ACK

在此之前，Case business progress 維持 `0005-010 PENDING`。
