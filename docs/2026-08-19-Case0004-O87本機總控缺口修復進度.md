# Case 0004：O87 最新本機總控缺口修復進度

- 日期：2026-08-19（Asia/Taipei）
- Case：0004 / DWG → 結構辨識 → 3D → AI-OpenSees
- 固定機：DESKTOP-O87PJNR（O87）
- Workspace：D:\AI-Work\jobs\0004-DWG-TO-3D
- 原則：案例實跑遇到缺口就補產品缺口，不繞過、不以舊 GitHub business Action 假閉環。

## 1. 最新控制面

```text
OpenWorker :8787
  -> gtr-local-exec
  -> go-tool-runtime :8848 durable local-work queue
  -> 4 claim workers + 4 execution workers
  -> allowlisted local capability
```

GitHub Actions 只用於 code repair / deployment / verification，不作 Case business scheduler。

## 2. 已完成產品缺口修復

1. `696c42b8...`：server API import 回到 canonical `pkg/api`。
2. `b8db6bf5...`：移除 stale import。
3. `62b5f4a9...`：local supervisor bootstrap 與 optional tool checkout 解耦。
4. `275577ba...` / `81f7082a...` / `0f37b809...`：REAL-host 與 generic tests authority 隔離。
5. `22b20f31...`：deployment success 必須綁定 REAL 4+4 verifier。
6. `da6cac48...` / `938a3c0b...` / `aa0df0ff...`：修正 Windows UTF-8 BOM receipt 問題。
7. `f1f4b55c...`：REAL verifier 失敗原因完整輸出。
8. `2d11eba3...`：deployment latest-wins，避免 stale backlog 覆蓋新版本。
9. `c6dbd7df...`：claim-agent 遇 transient loopback/HTTP 錯誤不再拖死整個 4-slot process。

## 3. O87 已有 REAL 控制面證據

舊部署 run `32198262403` 的 O87 job 已完成 targeted tests、build、install、queue bootstrap、control-plane verification 與 deployment receipt；receipt 證明 `queue_configured=true`、queue=`127.0.0.1:8848`、target=`DESKTOP-O87PJNR:8848`、runtime=`detached-wmi-process`。

但這是 v4 control-plane receipt，只證明控制面安裝與存活，不等價於目前要求的 fresh REAL 4+4 OPERATIONAL。

歷史 REAL smoke 已曾實證 4 distinct claim workers、4 distinct executor slots、LOCAL_SUPERVISOR route 與五個 smoke work 完成；因此目前剩餘問題是 fresh source convergence / operational closure，不是 4-slot 模型本身不存在。

## 4. 2026-08-19 09:07+08:00 最新接續

已新增／刷新專用 maintenance convergence workflow：

```text
.github/workflows/case0004-o87-maintenance.yml
commit = 308a9ec697a7256ad935bca4a6880fe8153a622e
```

它只做控制面維護與驗證，不做 Case business execution：

1. 固定 hostname `DESKTOP-O87PJNR`；
2. refresh 到 newest main；
3. 安裝 newest authoritative local supervisor，`MaxParallelActions=4`；
4. 一次呼叫 `/api/execution/local-work/clear` 清 O87 authoritative queue；
5. 驗證 queued/inflight 均為 0；
6. 跑 REAL four-slot verifier；
7. 強制驗收 `REAL_VERIFIED`、`LOCAL_SUPERVISOR`、claim>=4、executor>=4、GitHub business execution=false；
8. 成功後寫 `evidence/deployments/case0004-o87-maintenance-latest.json`。

## 5. 2026-08-19 09:11+08:00 再觸發 convergence

09:10 查核時，`case0004-o87-maintenance-start.json` 與 `case0004-o87-maintenance-latest.json` 均仍未落盤，代表 09:07 版本尚未留下可接受的實機開始／完成證據。

因此直接再次刷新 workflow marker 觸發最新版 convergence：

```text
commit = 1f03996d079a492e5f8aeecbf05a999377487c4e
message = case0004: retrigger O87 maintenance convergence
marker = 2026-08-19T09:11+08:00
```

本次仍維持同一收斂語義：

```text
newest main
 -> install local supervisor
 -> one-call queue clear
 -> verify queue empty
 -> REAL 4-slot verifier
 -> claim >= 4
 -> executor >= 4
 -> LOCAL_SUPERVISOR
 -> maintenance receipt
```

截至本次文件寫回時，尚未觀察到新的 `maintenance: start Case0004 O87 <run_id>` commit，因此 **business dispatch gate 仍維持鎖定**，不以「已觸發」冒充「已執行」。

## 6. 當前 Gate

已完成：

- [x] canonical pkg/api
- [x] stale import cleanup
- [x] supervisor 與 optional tool checkout 解耦
- [x] generic queue / supervisor-status test isolation
- [x] O87 :8848 persistent control plane REAL 啟動過
- [x] REAL smoke 曾實證 4 claim + 4 executor
- [x] receipt BOM reader/writer 缺口已修
- [x] REAL failure diagnostics 缺口已修
- [x] stale deployment backlog latest-wins 已修
- [x] claim-agent transient disconnect whole-process fatality已修
- [x] Case0004 O87 專用 clear + REAL verify convergence workflow 已建立
- [x] 09:11 再觸發最新版 convergence

尚未解除 business dispatch gate：

- [ ] O87 接到最新 maintenance convergence run
- [ ] maintenance start receipt 落盤
- [ ] authoritative queue clear receipt：queued=0 / inflight=0 / stale=0
- [ ] fresh REAL verifier = `REAL_VERIFIED`
- [ ] route = `LOCAL_SUPERVISOR`
- [ ] fresh claim slots >= 4
- [ ] fresh executor slots >= 4
- [ ] `case0004-o87-maintenance-latest.json` 落盤

## 7. Gate 通過後立即接續

不重做已驗收成果，直接：

1. 向 OpenWorker 查 Case 0004 live canonical next READY step；
2. 向 go-tool 查該 step capability / inputs / restrictions / negative knowledge；
3. 只由 `LOCAL_SUPERVISOR` dispatch REAL business work；
4. 可 fan-out 的工作使用 4-slot 並行；
5. 持續 DWG → Story Index → 結構辨識 → 3D → AI-OpenSees → Artifact Registry → Drive Review → Delivery Revision → Final Acceptance；
6. 每一輪把 work id、claim worker、executor slot、PID、route、receipt、artifact SHA256、QC 結果同步回 Case 文檔 / ledger / Worklist。

## 8. 驗收鐵律

```text
source fixed != deployed
deployed != REAL 4+4 verified
process alive != OPERATIONAL
queue health != 4+4 verified
old event time != current source
control path success != business artifact acceptance
REAL smoke success != receipt/status closure
single-slot transient transport error != whole-supervisor fatal
GitHub maintenance workflow != GitHub business scheduler
triggered != started != completed
```

只有 fresh maintenance receipt + REAL 4+4 + LOCAL_SUPERVISOR 同時成立，Case 0004 才解除 business dispatch gate。