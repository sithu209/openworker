# 案例 0004：DWG → 3D O87 固定 Runner REAL 進度

時間：2026-08-17 14:02（Asia/Taipei）  
狀態：`IMPLEMENTING / CANONICAL SOURCE MATERIALIZED / CAD OPEN REAL PASS / INSPECT+RENDER RUNNING`

## 1. 最新固定機器路由

案例 0004 現在正式使用：

```text
GitHub Actions
  runs-on: [self-hosted, Windows, X64, O87]
  ↓
COMPUTERNAME == DESKTOP-O87PJNR  二次 fail-closed
  ↓
直接執行 owning repo canonical entrypoint
```

本案例不再依賴 per-host local queue、Tailscale queue 或 shared work queue 作為正式 business routing。

## 2. Canonical REAL source 已物理落地

主輸入：

```text
original: S1-1140926(1).dwg
canonical: D:\AI-Work\jobs\0004-DWG-TO-3D\input\source.dwg
size: 1,385,583 bytes
header: AC1032
SHA256: aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6
host: DESKTOP-O87PJNR
```

`Case 0004 O87 Local Open DWG` run `31999649131` 在執行 CAD 前重新讀取 physical file，size / SHA256 / AC1032 全部一致，因此 canonical source materialization 已有獨立下游 physical gate 證明。

## 3. OpenWorker local ingress 真實暴露並修復兩個缺口

### 3.1 API signature drift

O87 REAL ingress 首次進入最新版 OpenWorker 後，暴露：

```text
start_isolated_os() 呼叫簽名漂移
EngineeringSourceIngress 舊 ingest_local_file API 漂移
write_github_outputs() evidence_path 漂移
```

Owning repo：`liuxb99/openworker`

修復：

```text
commit: 77faf411...
message: fix(ingress): align local source entrypoint with canonical API
```

修復後 local-source entrypoint 重新共用 canonical：

```text
start_isolated_os(os_root, workspace, port)
EngineeringSourceIngress(...).ingest(...)
write_github_outputs(result, evidence_path)
```

### 3.2 Windows CP950 output encoding

修復簽名後，O87 REAL ingress 已進到更深層並完成 canonical source 寫入，但最後 console/output 出現：

```text
'cp950' codec can't encode character '\ufffd'
```

這不是 DWG bytes 失敗；下游 run 已獨立證明 canonical source 完整存在且 SHA 正確。

Owning repo：`liuxb99/openworker`

修復 commit：

```text
49650918dfbba02c4f2b3dfd96987e06bc19ceb0
fix(ingress): force UTF-8 on Windows local source runtime
```

修復內容：

```text
PYTHONUTF8=1
PYTHONIOENCODING=utf-8
stdout/stderr reconfigure UTF-8 + backslashreplace
```

目前 clean ingress rerun 已重新排入 O87；它的目的只是把 workflow evidence 從「業務結果成功、最後輸出紅」收成完整 green，不再重新定義 source identity。

## 4. cad.open_dwg REAL 已完成

Owning repo：`liuxb99/DWG_todo`

正式 workflow 已從舊 queue adapter 改為 O87 fixed runner 直接執行：

```text
scripts/invoke-agent-cad-local.ps1
  -Method cad.open_dwg
  -WorkspaceRoot D:\AI-Work\jobs\0004-DWG-TO-3D
```

成功 run：

```text
run_id: 31999649131
job_id: 95297462463
host: DESKTOP-O87PJNR
conclusion: success
```

REAL receipt：

```json
{
  "revision": 2,
  "scope": "default",
  "size": 1385583,
  "source_path": "D:\\AI-Work\\jobs\\0004-DWG-TO-3D\\input\\source.dwg",
  "source_sha256": "aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6"
}
```

Evidence：

```text
D:\AI-Work\jobs\0004-DWG-TO-3D\evidence\dwg-cad-local\case0004-open-31999649131
```

所以：

```text
canonical source physical identity = PASS
cad.open_dwg REAL = PASS
```

## 5. O87 workflow 編排缺口已修

原本 downstream `open-dwg` 會先佔住 O87 runner 等待 `source.dwg`，若 O87 當下只有一個 matching runner，就會阻塞上游 ingress。

已修為：

```text
source 不存在 → 立即 fail
source 已存在 → 立即執行
```

不允許 downstream 長時間佔 runner 等 upstream artifact。

DWG_todo commit：

```text
2e1010621bdd4e4ccf8b088ece8df77091f346a0
fix(case0004): fail fast when ingress has not materialized source
```

## 6. Model extents + overview REAL 正在執行

新 workflow：

```text
.github/workflows/case-0004-o87-inspect-render.yml
```

commit：

```text
cd46ef70493b01cc8979450a3d7adaa4c8cce7f9
feat(case0004): inspect and render canonical S1 DWG on O87
```

REAL run：

```text
run_id: 31999806833
job_id: 95297871294
host: O87 fixed runner
current step: cad.get_model_extents REAL
```

此 run 的 gate：

```text
source SHA 必須等於 aaadbd84...
entity_count > 0
model bounds 必須存在且寬高 > 0
cad.render_png = 2400 x 1600
PNG physical file 必須存在且非空
PNG SHA256 必須與 receipt 一致
metadata JSON 必須存在
```

成功後會寫：

```text
D:\AI-Work\jobs\0004-DWG-TO-3D\evidence\case0004-inspect-render.json
```

## 7. OpenCAD 重複編譯效能缺口已補

目前 isolated checkout 每次跑 `cad.get_model_extents / render_png / candidate scan` 時，若沒有 machine `DWG_OPENCAD_BIN`，會重新編譯 OpenCAD release binary。

已在 `DWG_todo/scripts/invoke-agent-cad-local.ps1` 補 commit-keyed ProgramData authority cache：

```text
C:\ProgramData\DWG_todo\opencad\<dwg-commit-12>\opencad-case.exe
C:\ProgramData\DWG_todo\opencad\<dwg-commit-12>\authority.json
```

commit：

```text
16485d6780e8ee036088280e0189afec0cbfc9d5
perf(case0004): cache OpenCAD authority by DWG commit
```

後續同一 DWG_todo commit 的 visual scan / Story Region / Building DWG validation 可直接 cache hit；不同 commit 仍會重新 build，避免錯用 stale binary。

## 8. 下一步

只有拿到本次 REAL overview metadata 後才進 Story Region，不先猜樓層範圍。

正式順序：

```text
cad.get_model_extents
→ cad.render_png
→ inspect overview metadata / candidate_modeling_views
→ cad.list_candidate_regions
→ 根據真實 bounds / handles 選 Story Region
→ cad.set_story_region
→ cad.query_bounds
→ Story Design / review / materialize
→ Building World Coordinates
→ building GLB
→ Blender REAL reopen/render
→ native AC1032 ACIS Solid3D DWG
→ reopen / 3DSOLID / layer validation
→ OS Artifact Registry
→ Review / Delivery
```

截至本文件更新：

```text
[PASS] REAL user DWG identity
[PASS] canonical source.dwg physical bytes on O87
[PASS] cad.open_dwg REAL on O87
[RUNNING] model extents + 2400x1600 overview
[PENDING] Story Region onward
```
