# Case 0005 白雪公主：本機執行進度與操作紀錄

更新時間：2026-08-19 20:50 +08:00

> 本文件為 Case0005 持續追加的操作紀錄。既有完整流程、revision 15、Generic Go Case Engine、4+4 本機總控與成果驗收規則維持有效；本次新增 2026-08-19 20:48 REAL Embedded Control 重跑證據。

## 2026-08-19 20:48 REAL Embedded Control 重跑證據

### 1. 本次目的

針對「Case0005 密語已送出但當輪沒有 smoke receipt」重新確認：

1. ODA self-hosted runner 是否仍能接單；
2. `OPENWORKER_CONTROL` 是否仍能被 Hook 偵測；
3. Hook 是否仍能把 control envelope 交給 OpenWorker；
4. `CASE.STATUS` / `SUPERVISOR.STATUS` / request-id 幂等是否仍可用；
5. 只有上述 read-only 控制鏈重新確認後，才進入真正會改狀態的 `QUEUE.CLEAR + CASE.CONTINUE_BATCH`。

### 2. 已取得的不可變 REAL 證據

重跑既有成功 Run：

- workflow run：`32247344666`
- rerun job：`96073210405`
- job name：`ODA embedded-control 9-round stability`
- runner：`DESKTOP-ODAQN0D-R001`
- machine：`DESKTOP-ODAQN0D`
- runner version：`2.336.0`
- conclusion：`success`

本次不是停在 queued；ODA 已實際 claim job 並完成。

### 3. 9/9 控制輪結果

REAL 日誌輸出：

- `total_rounds = 9`
- `successful_rounds = 9`
- 5 × `CASE.STATUS`：PASS
- 2 × `SUPERVISOR.STATUS`：PASS
- 2 × 相同 request-id `CASE.STATUS` 幂等驗證：PASS

每輪 Hook 均明確輸出：

```text
[OpenWorker Hook] OPENWORKER_CONTROL detected
[OpenWorker Hook] control envelope accepted by OpenWorker
```

單次控制延遲約 `484–661 ms`。

因此本次可以正式記錄：

```text
GitHub Actions ingress        PASS
ODA self-hosted runner claim  PASS
OPENWORKER_CONTROL detection  PASS
OpenWorker Hook               PASS
Dispatcher/OpenWorker accept  PASS
CASE.STATUS                   PASS
SUPERVISOR.STATUS             PASS
request-id idempotency        PASS
```

### 4. 重要邊界

這次 rerun 是既有 **read-only 9-round stability** job，來源為歷史成功驗證路徑；它證明 ODA / Hook / Embedded Control 現在仍然可用，但不能據此宣稱：

- `QUEUE.CLEAR` 已於本輪執行；
- `CASE.CONTINUE_BATCH` 已於本輪執行；
- Case0005 新 business work 已建立；
- 4 claim + 4 executor 已於本輪開始 business fanout；
- 白雪公主 PPTX 已生成。

上述項目仍需要 mutating REAL evidence / durable work / artifact evidence。

### 5. 下一個正式動作

倉庫已有正式 mutating harness：

`script/test-openworker-embedded-control-mutating-stability.ps1`（實際路徑為 `scripts/test-openworker-embedded-control-mutating-stability.ps1`）

其順序為：

```text
CASE.STATUS
→ SUPERVISOR.STATUS
→ QUEUE.CLEAR
→ QUEUE.CLEAR
→ SUPERVISOR.STATUS
→ CASE.CONTINUE_BATCH
→ 相同 request-id 再送一次 CASE.CONTINUE_BATCH（驗證 durable idempotency）
→ CASE.STATUS
→ CASE.STATUS
→ SUPERVISOR.STATUS
```

成功後本機 evidence：

`C:\ProgramData\OpenWorker\hooks\mutating-stability-latest.json`

下一步必須取得這條 mutating REAL run 的實機結果；若 continue 後 durable work 已為 `pending / claimed / running`，禁止再重複 continue，只查狀態；若進入 READY fanout，才檢查 4 claim + 4 executor 的實際並發。

### 6. Case0005 成果 gate 不變

業務主鏈仍以 source worklist / Case Engine 為 authority，且不得因控制鏈 PASS 就跨越成果 gate：

```text
0005-010 Director
→ 0005-020 storyboard request
→ 0005-025 text-only storyboard PPTX
→ 0005-026 Drive publish
→ 0005-027 使用者 APPROVE/REJECT
→ 0005-030 character fanout + 0005-040 scene fanout
→ 後續 illustrated storyboard / video / delivery
```

只有真實 artifact、manifest/hash、Case status/ledger 綁定與必要 Drive review 都存在時，才可宣稱對應業務 step 完成。

## 既有 Case0005 Authority 摘要

- Case ID：`0005`
- 名稱：`Snow White / 白雪公主`
- 固定執行機器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- Source Worklist authority：`case-worklists/0005.json`
- Runtime snapshot：ODA workspace `.openworker/case-worklist.json`
- Canonical control plane：`openworker.exe`
- Case Engine：`OpenWorker Go Case Engine`
- Durable queue / executor authority：`go-tool-runtime :8848`
- Resident OpenWorker：`127.0.0.1:8787`
- 本機總控目標：`REAL_VERIFIED + OPERATIONAL + 4 claim slots + 4 executor slots`
- GitHub Actions：只作 transient command transport / install / read-only evidence transport，不作 Case business execution / queue / artifact authority。
- Google Drive：成果發布與 ChatGPT / 使用者審查，不作業務命令入口。

## 操作鐵律

1. 流程問題問 OpenWorker；工具問題問 go-tool。
2. GitHub 是 transport，不是 supervisor。
3. active work 為 `pending / claimed / running` 時禁止 duplicate `case continue`。
4. `QUEUE.CLEAR` 只在確認需要清理阻塞時使用；清完必須 readback supervisor/case status。
5. mutating control 必須有唯一 request-id；重放同 request-id 必須命中 durable receipt。
6. 多個 READY work 的 fanout 由 OpenWorker Local Supervisor 決定，不由 ChatGPT/GitHub workflow 硬編 business fanout。
7. 沒有 durable work / ledger / artifact evidence，不得宣稱 Case 業務完成。
8. 0005-027 等 approval gate 未取得使用者明確批准前不得跨越。
