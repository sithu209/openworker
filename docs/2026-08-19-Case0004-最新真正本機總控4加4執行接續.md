# Case 0004：切換至最新「真正本機總控 + 4+4 並行 Action」執行接續

- 日期：2026-08-19（Asia/Taipei）
- Case：`0004` / DWG → 結構辨識 → 3D → AI-OpenSees
- 固定機：`DESKTOP-O87PJNR`（O87）
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 文件性質：**Case 0004 最新執行權威補充／切換聲明**
- 前版主手冊：`docs/2026-08-18-1412-Case0004-本機總控並行Action遷移與執行手冊.md`
- 最新架構依據：`docs/2026-08-19-真正本機總控實作完成待ODA實機驗證.md`

## 0. 權威聲明

自本文件起，Case 0004 後續 REAL business execution **必須使用 2026-08-19 最新版真正本機總控功能**。

若 2026-08-18 的 Case 0004 舊手冊、舊 workflow、舊腳本說明與本文件衝突，**以本文件與當前最新 OpenWorker / go-tool-runtime 源碼 contract 為準**。

這次不是重新開始案例，也不是把舊 GitHub Action 換一個名稱；而是把 Case 0004 的執行控制面正式切到最新版：

```text
ChatGPT / Case Supervisor
        |
        v
OpenWorker :8787
  Case DAG / process job / PID / timeout / cancel / execution explain
        |
        v
gtr-local-exec compatibility client
        |
        v
go-tool-runtime :8848
  durable local-work queue
        |
        +-- claim worker 01
        +-- claim worker 02
        +-- claim worker 03
        +-- claim worker 04
        |
        +-- execution slot 01
        +-- execution slot 02
        +-- execution slot 03
        +-- execution slot 04
        |
        v
allowlisted local capability
```

## 1. 「最新版本」在 Case 0004 的確切意思

Case 0004 後續必須同時使用下列最新版能力：

1. **OpenWorker :8787 作為 Case / process authority**
   - Case DAG；
   - durable child process job；
   - PID；
   - timeout / cancel；
   - execution explain；
   - Case runtime / ledger。

2. **go-tool-runtime :8848 作為本機 Action queue / parallel scheduler / tool authority**
   - durable local-work queue；
   - claim / lease / heartbeat；
   - execution lock；
   - stale recovery；
   - tool capability authority；
   - 4 個 claim workers；
   - 4 個 execution workers。

3. **gtr-local-exec 不得繞過 :8848 直接執行 capability**

   Canonical route：

   ```text
   gtr-local-exec
     -> POST :8848/api/execution/local-work
     -> durable queue
     -> claim
     -> execution slot
     -> terminal receipt
   ```

4. **GitHub Actions 不得作為 Case 0004 business execution bus**
   - 可以：code update、文件、CI、regression、repair、必要 emergency transport；
   - 不可以：正常 Case business scheduler、長任務 owner、progress authority。

## 2. 4+4 並行的定義

本文所稱「4+4」不是 8 個互不相關的 business workers，而是最新版真正本機總控的兩層 worker pool：

```text
4 x claim workers
+
4 x execution workers
```

每個 durable local work 必須有可追溯的：

- work id；
- claim worker id；
- execution slot；
- lease / heartbeat；
- PID；
- attempt；
- duration；
- submitted / claimed / execution_started / execution_finished / completed 或 failed events。

當 DAG dependency 與 resource lock 允許時，Case 0004 的可拆工作要真正 fan-out，不再被 GitHub runner queue 人為串行化。

## 3. Case 0004 固定機與路由

Case 0004 固定：

```text
machine = DESKTOP-O87PJNR
workspace = D:\AI-Work\jobs\0004-DWG-TO-3D
required_execution_route = LOCAL_SUPERVISOR
```

正常 business work 必須滿足：

```text
route_label = LOCAL_SUPERVISOR
github_action_used_for_business_execution = false
```

若查到 `GITHUB_ACTIONS` 或 `UNKNOWN`，不得把該次執行當成最新版 Case 0004 的有效 business execution。

## 4. 派工前 O87 實機驗收門檻

最新版源碼已具備真正 4-slot supervisor 能力，但 Case 0004 在 O87 上開始 REAL business work 前，必須先確認 O87 本機安裝與運行狀態。

至少驗收：

1. `tool-runtime.exe`、`gtr-work-agent.exe`、`gtr-work-executor.exe`、`gtr-local-exec.exe` 為同一最新版；
2. 本機 queue authority 顯示：
   - claim workers = 4；
   - execution workers = 4；
3. 以無副作用 smoke capability 驗證真正並行；
4. 實測 peak active slots = 4；
5. 至少 4 個不同 claim worker IDs；
6. 至少 4 個不同 executor slots；
7. smoke work 全部走 `LOCAL_SUPERVISOR`；
8. durable events 可完整還原 submitted → claimed → execution_started → execution_finished → completed；
9. O87 Case runtime 查詢可返回 machine/case active slots、queued、inflight 與 work IDs。

**沒有 O87 實機 4+4 receipt 前，只能稱 SOURCE IMPLEMENTED / O87 REAL VERIFICATION PENDING，不得宣告 Case 0004 最新版已 REAL VERIFIED。**

## 5. 舊 Case 0004 成果不因架構升級失效

本次切換只更換 execution/control plane，不應因升級而抹掉已存在且能被實體 evidence 證明的 Case business 成果。

原則：

```text
已驗收 artifact / SHA256 / receipt
        -> 保留
        -> 不為了驗證新版控制面而重做

未完成 / REWORK / 缺 receipt 的 step
        -> 從 canonical next step 原地接續
        -> 用最新版 LOCAL_SUPERVISOR route 執行
```

禁止為了「測最新版」把 Case 0004 從第一步整案重跑。

## 6. Worklist 與舊手冊的遷移規則

目前 `case-worklists/0004.json` 已包含本機 supervisor / 4-slot / GitHub business scheduler=false 的方向，但其 authority 字樣仍屬上一版 contract。

Case 0004 下一次正式寫回 Worklist 時，應對齊最新版語義：

```text
parallel_policy.authority = go-tool-runtime-local-supervisor
max_local_slots = 4
required_execution_route = local_supervisor
process_kernel = openworker-local-supervisor
github_business_scheduler = false
```

其中：

- `go-tool-runtime-local-supervisor` = Action queue / parallel execution authority；
- `openworker-local-supervisor` = Case process / DAG kernel；
- 兩者不是競爭的兩套 scheduler，而是上下兩層權威分工。

## 7. Case 0004 後續操作固定規則

使用者說「繼續做案例 4」時，後續固定按最新版做：

```text
1. 先讀 Case 0004 最新文檔 / Worklist / durable evidence
2. 用 OpenWorker 問 Case 進度與 process/runtime state
3. 用 go-tool 問當前 step 的 capability、參數、限制與負面知識
4. 驗證 O87 的 LOCAL_SUPERVISOR route 與 4+4 runtime
5. 若 queue 阻塞，使用本機 queue admin / drain，不排一個 GitHub Action 去清另一個 queue
6. dependency 滿足後，POST durable local-work
7. 可 fan-out 的工作直接使用 4-slot 並行
8. control path 接受只證明控制面已通；business acceptance 仍須真實 artifact / receipt / SHA256 / QC
9. 將 execution route、work id、slot、PID、receipt、artifact evidence 寫回 Case ledger / Worklist
10. 再進下一個 READY step
```

## 8. 舊 GitHub workflows 的定位

現有 `.github/workflows/case-0004-*` 不刪除，保留作：

- 歷史 provenance；
- regression；
- repair / code update；
- emergency fallback evidence。

但從本文件生效後，**不得因為舊 Case workflow 還存在，就把它當作 Case 0004 正常 business execution 的 canonical route。**

## 9. 本次切換驗收

文件層驗收：

- [x] 明確指定 Case 0004 使用 2026-08-19 最新真正本機總控；
- [x] 明確記錄 OpenWorker :8787 與 go-tool-runtime :8848 的最新版分工；
- [x] 明確記錄 4 claim + 4 execution 的 4+4 模型；
- [x] 明確禁止 GitHub Actions 作正常 business execution；
- [x] 明確固定 O87 / workspace；
- [x] 明確要求 O87 先做 REAL 4+4 驗證；
- [x] 明確規定舊 evidence 保留、從 canonical next step 原地接續；
- [x] 明確規定 control-path success 不等於 business artifact acceptance。

執行層狀態：

```text
CASE0004 LATEST TRUE LOCAL SUPERVISOR CONTRACT DOCUMENTED
O87 REAL 4+4 VERIFICATION PENDING
BUSINESS DISPATCH NOT STARTED BY THIS DOCUMENT UPDATE
```

## 10. 2026-08-19 07:49+08:00～：案例實跑發現缺口與補缺口紀錄

本段是 Case 0004 實跑後新增的產品缺口紀錄。原則：**案例遇到缺口，不繞過；先把缺口補進產品，再回案例驗證。**

### 10.1 發現的缺口

O87 已由部署 workflow 正確命中固定機：

```text
EXPECTED_HOST = DESKTOP-O87PJNR
actual host   = DESKTOP-O87PJNR
host check    = PASS
```

但最新版 local supervisor 部署在 targeted tests 階段 fail-closed：

```text
internal/server/server.go:13:2:
package go-tool-runtime/internal/api is not in std
```

因此該次部署的：

- build additive local tools；
- install additive local tools；
- durable local queue bootstrap；
- persistent local-work control-plane verification；
- deployment receipt；

全部不得視為成功。

### 10.2 根因

`internal/server/server.go` 的 API DTO import 漂移成：

```go
"go-tool-runtime/internal/api"
```

但目前 canonical API package 實際位於：

```go
"go-tool-runtime/pkg/api"
```

`internal/project/manager.go` 亦使用 `pkg/api`，因此這不是要新增另一套 API DTO，而是 server import path drift。

### 10.3 已補產品缺口

已在 `liuxb99/go-tool-runtime` / `main` 修正 `internal/server/server.go`：

```text
internal/api -> pkg/api
```

修復 commit：

```text
696c42b8c55fe0a9c2af144c9caa8fe9e65a843f
fix: restore server API import to canonical pkg/api
```

修復採最小變更，不新增重複 DTO，不改 OpenWorker / go-tool-runtime 權威分工，也不繞過 targeted tests。

### 10.4 回案例驗證

已要求重新執行 O87 的 local executor deployment job。

重新執行的 O87 job：

```text
run_id = 32198262403
new job_id = 95908698409
machine = DESKTOP-O87PJNR
status at record time = QUEUED
```

後續 fail-closed 驗收：

```text
1. targeted tests 必須 PASS
2. build/install 必須 PASS
3. :8848 durable queue 必須健康
4. 最新 4 claim + 4 execution heartbeat 必須 fresh
5. REAL smoke 必須 peak_active_slots = 4
6. route 必須全部 LOCAL_SUPERVISOR
7. verification receipt 必須存在且為最新
8. 才能恢復 Case 0004 business dispatch
```

截至本次文件更新：

```text
PRODUCT GAP FOUND = YES
PRODUCT GAP FIXED IN SOURCE = YES
O87 REDEPLOY REQUESTED = YES
O87 REAL 4+4 VERIFIED = NO / PENDING
CASE0004 BUSINESS DISPATCH = BLOCKED UNTIL REAL VERIFICATION
```
