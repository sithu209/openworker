# 案例 0005 白雪公主：本机优先分镜资产并行闭环进度

时间：2026-08-18 15:41 +08:00

状态：030/040/050 IMPLEMENTED — ONE-SHOT ODA RECOVERY + LOCAL CONTROLLER START TRIGGERED

## 当前事实

Case 0005 的权威 Worklist 为 `case-worklists/0005.json`，固定机器 `DESKTOP-ODAQN0D`，业务工作目录 `D:\AI-Work\jobs\0005-SNOW-WHITE`。

当前 DAG：

- `0005-010`：Comfyx-Studio Director preproduction。
- `0005-020`：生成 storyboard request 与 visual asset requirements。
- `0005-025`：OpenMAIC 生成无图文字分镜 PPTX。
- `0005-027`：等待用户批准文字分镜 PPTX。
- `0005-030`：批准后生成 REAL character master images。
- `0005-040`：与 030 并行生成 REAL scene concept images。
- `0005-050`：030/040 join 后，在一个原子 local capability 内生成 REAL shot storyboard images、持久化每个 IMAGE receipt，再 bind 回 storyboard request。
- `0005-055`：生成 illustrated storyboard PPTX。
- `0005-057`：第二次用户批准。
- 后续进入逐 shot REAL video fan-out、finalize、WorkLedger、Drive review、LLM review、Delivery Revision。

## 已关闭的缺口

### 1. 移除 nested GitHub business execution

旧 `case0005_parallel_bootstrap.ps1` 会在 OpenWorker durable job 内再次调用 GitHub workflow，违反 local-first。现已改为：GitHub 只做外部唤醒/救援 transport；真正业务执行路径是：

`coworker.case0005_controller -> OpenWorker Go durable job -> gtr-local-exec -> ComfyX / Comfyx-Studio / OpenMAIC`

### 2. 失败 evidence 必须可审计

Case 0005 transport 已要求成功/失败都持久化 node/jobs/Worklist/controller/error，再 fail-closed，避免失败后没有学习事实。

### 3. 030/040 权威 role 分支

go-tool `image.comfyx.storyboard-real` 已支持 `role` 批量执行，只允许：`character_master / scene_concept / shot_storyboard`。asset_id 一律从真实 `visual-assets/requirements.json` 取得，不猜 ID，不写死 ComfyUI graph/model/sampler。

030 与 040 是两个独立 OpenWorker durable jobs，可占不同 local slot 并行；没有第二套 scheduler。

### 4. 050 REAL shot image + bind 原子闭环

新增 go-tool capability `comfyx-studio.storyboard.real-bind`。一个 OpenWorker durable job 内依次完成：shot_storyboard REAL IMAGE -> artifact/SHA 校验 -> receipt 文件落盘 -> Studio bind -> bound request 实体验证；任一环失败整步 fail-closed。

Worklist revision 4 已把 `0005-050.allowed_actions` 收敛成这个单一原子 capability。

## 本轮关键提交

- `fa092db9de0a2d5841b87ba751beb7dedc830699` — go-tool role batch IMAGE。
- `3dfc90ac9555422f6c78f0825fe2962c094d8f37` — Case 0005 专用 controller。
- `1842b47b18853ba1246049bb6cbcbb2540721144` — REAL shot image + bind composite。
- `55a3ad93467d06fb7c96b01de02abb5b17f072f3` — 注册 composite capability。
- `fc48389275f39abc1416480f18a4991a3c7a6a68` — Worklist revision 4。
- `fae72131f825d67f79ea6827244a467c1a3b547c` — Case controller 接 050。
- `5a1439b54559acfe448f4cb4ead4159c1f3a8eb1` — ODA canonical OpenWorkerNode bootstrap 增加窄 push marker 入口。
- `f3fbed78e059698fd7ae53a90f6f99a3d089f0c4` — 新增一次性 `Case 0005 ODA Recover And Local Start` 救援 transport。

## 当前恢复动作

因为此前 Case 0005 没有形成任何 `case-evidence/0005` durable fact，本轮不再分两次 transport。一次性救援流程会在 ODA：

1. 固定检查 `DESKTOP-ODAQN0D`。
2. 编译最新 OpenWorkerNode。
3. 安装/升级 Windows 常驻服务，4 workers。
4. 验证 8787 node authority 与 `case0005 / comfyx / local-supervisor` inventory。
5. 直接执行 `scripts/case0005_parallel_bootstrap.ps1`。
6. 本机 Case controller 提交 `0005-010` durable job 后，Action 的业务职责结束。
7. 后续 `010 -> 020 -> 025 -> 027` 由 ODA 本机 OpenWorker durable jobs 接续，不再经过 GitHub。
8. 无论 start 成功或失败，都回写 `case-evidence/0005/oda-recover-start.json`。

本次提交带 `[case0005-recover-start]` 触发上述一次性救援。

## 恢复后的自动接续点

到 `0005-027` 必须停下，向用户交付并展示**无图文字分镜 PPTX**，等待明确批准；不得自动跨越审批门。批准后才允许 030 与 040 并行，随后 050 原子 REAL shot image + bind。

## 验收边界

`Action completed` 不等于 `Case step succeeded`。Case 0005 每一步只接受 OpenWorker Worklist durable state + localexec REAL receipt + artifact/hash/QC evidence。
