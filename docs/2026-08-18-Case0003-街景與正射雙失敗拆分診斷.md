# Case 0003 玉井橋：街景與正射影像雙失敗拆分診斷

日期：2026-08-18
狀態：REWORK / REAL VERIFICATION IN PROGRESS

## 當前結論

Case 0003 目前必須同時視為兩個獨立 blocker：

1. Street View browser evidence 失敗；先前實體結果接近全黑，semantic visibility gate 正確拒絕。
2. NLSC orthophoto / SceneX textured assembly 尚未形成可驗收成功鏈；舊串行 driver 在 Street View 失敗後會直接停止，使正射影像沒有被獨立執行與診斷。

不得把任一舊檔、缺檔或前一版 SceneX 截圖視為修復成功。

## 新版 OpenWorker queue-drain

最新版 OpenWorker 已提供一次呼叫的即時 queue drain：

```text
openworker queue-drain --capability terrain.streetview.acquire
```

這不是 Case Worklist 業務步驟。一次呼叫必須完成 query / cancel / retry / verify，只有 clean=true 且 remaining_active=[] 才可繼續。

Case 0003 新 imagery REAL 入口會在 Street View dispatch 前直接執行這個正式能力，不再自行拼接 queue admin 流程。

## 兩條影像鏈拆分

AI-Engineering-OS 新增：

```text
scripts/case0003_imagery_repair_ul7_split.py
```

Street View 與 NLSC orthophoto 現在獨立執行：

```text
OpenWorker queue-drain terrain.streetview.acquire
  -> REAL Street View reacquire
  -> Street View semantic gate

同一次 Case run：
  -> REAL NLSC PHOTO2 acquisition
  -> site-imagery/v1 physical SHA gate
  -> scenex-textured-assembly/v1 physical SHA/bounds gate
```

其中一條失敗不得阻止另一條執行；最後 summary 才合併 verdict。任一條失敗，整體 imagery 仍 fail-closed。

## REAL 執行入口

AI-Engineering-OS：

```text
.github/workflows/case-0003-imagery-split-repair-ul7.yml
```

固定 host：DESKTOP-UL7V2VV / UL7。

特性：

- 保留已驗收 geo / DTM / AOI / Blender，不重跑。
- Case-local go-tool 使用 18848，避免與其他案例共用 8848。
- 使用最新版 OpenWorker one-call queue drain。
- Street View 與 orthophoto 各自留下 failure JSON / physical evidence。
- 成功時必須同時看到 Street View semantic_visible=true 與 orthophoto textured assembly PASS。

## 後續驗收

只有兩條影像鏈都成功後才允許進入 SceneX imagery REAL browse：

1. 四方向 Street View 實體圖片必須可見，不得近黑。
2. NLSC PHOTO2 實體 texture 必須存在、非空、SHA256 一致。
3. scenex-textured-assembly/v1 必須有有效 local_bounds_xz。
4. SceneX 必須以 --require-imagery 載入正射影像，不得退回純色材質。
5. 最終 1280x720 SceneX screenshot 必須人工目視確認正射影像方向、位置與地形一致。
6. 玉井橋/模型可見性仍是最終 Case acceptance 的必要條件。

在上述條件完成前，Case 0003 不得進入新的 Drive review / WorkLedger PASS / Delivery Revision。

## 2026-08-18 11:30+08 REAL 拆分診斷與修復進度

最新一次已回寫診斷分支的 REAL run 為 AI-Engineering-OS `32094940031`（DESKTOP-UL7V2VV）。本次證據確認基礎設施不是 blocker：

- OpenWorker one-call queue drain 已成功，`clean=true` 且 `remaining_active=[]`。
- Case-local go-tool 18848 已成功啟動並通過 readiness / config contract。
- 已驗收的 geolocation / DTM / AOI / Blender 繼續保留，本輪沒有重跑。

本次 REAL run 暴露兩個獨立 owning-repo 缺口：

1. Street View：`Terrain_To_DXF` workflow run `32095027033` 在 Go 編譯階段失敗；`internal/streetview/tiles_stitch.go` 的 tile-height mismatch `fmt.Errorf` 少傳 `index.TileHeight`。已在 `Terrain_To_DXF/main` 修復，commit `019f0125d67cb8e07bc414154ff1c28a10e3d2fe`。
2. NLSC orthophoto：OS strict JSON decoder 拒絕舊欄位 `minLon`。目前權威 `AcquireImageryBounds` schema 為 `min_x / min_y / max_x / max_y`。已在 `AI-Engineering-OS/main` 修正 Case 0003 caller，commit `b5469547a5d3eb24ae42ebfbcfacf2916ce8602c`。

另補一個驗證流程缺口：原 workflow push paths 只監聽 split wrapper，沒有監聽真正的 `scripts/case0003_imagery_repair_ul7.py`，會造成實作修復後不自動做 REAL 回歸。已加入該路徑，commit `84a9e155b76fba620bc125f06221bd9666fa8711`。

### 目前權威狀態

截至本次文檔同步，固定診斷分支 `case0003-imagery-split-diagnostic/run.json` 仍指向舊 run `32094940031`，尚未取得上述修復後的新 REAL run 證據。因此 Case 狀態仍必須維持 `REWORK / REAL VERIFICATION IN PROGRESS`，不得提前宣告 imagery PASS。

下一個允許動作只有：等待/取得新 imagery split REAL 結果，分別驗證 Street View semantic gate 與 NLSC PHOTO2 + `scenex-textured-assembly/v1` physical gate；兩條都 PASS 後才進 SceneX `--require-imagery` REAL browse 與實體 screenshot 品質審查。Drive review / WorkLedger / Delivery Revision 仍保持 PENDING。
