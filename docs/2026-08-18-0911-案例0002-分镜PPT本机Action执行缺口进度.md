# 案例 0002：分镜 PPT 本机 Action 执行缺口进度

更新时间：2026-08-18 10:24（Asia/Taipei）

## 0. 案例记录铁律

从本次继续执行开始，案例 0002 的每一个步骤都必须在本文件留下详实记录。不得只写“已修”“已跑”“PASS”。每个步骤至少记录：

1. 时间与步骤编号；
2. 当时从 OpenWorker 读到的 canonical next step、dependencies、allowed_actions、acceptance、assigned_host；
3. 向 go-tool 提出的实际问题/task；
4. go-tool `/agent/query` 返回的 capability catalog / selected / readiness；
5. 最终选择的 capability、owning repo、workflow、ref、canonical inputs；
6. 本机 Action run id / job id / run attempt / COMPUTERNAME；
7. 实际工具输出、workspace artifact 路径、大小、SHA256、manifest/receipt；
8. 实体 reopen/QC/OOXML media 等验收；
9. OpenWorker 写回的 evidence、PASS/BLOCKED 与下一 canonical step；
10. 若失败：原始错误、真正 owning layer、修复 commit、永久测试、同一路径重跑结果。

任何没有机器证据的动作必须明确标为 `IMPLEMENTED / NOT LIVE VERIFIED`，不得写成 REAL PASS。

## 1. 当前产品流程

```text
Director / storyboard plan
→ 0002-025 无配图 storyboard PPTX
→ 0002-027 用户审批
→ 0002-030/040/050 REAL 图片与绑定
→ 0002-055 带配图 storyboard PPTX
→ 0002-057 用户审批
→ 0002-060 REAL 视频
```

任何 IMAGE / VIDEO 都不得越过对应用户审批。

## 2. 大模型正确工作方式

```text
用户要求继续案例
→ 查询 OpenWorker durable CaseWorklist
→ canonical_next_step_id / allowed_actions / acceptance
→ go-tool POST /agent/query
→ Action capability directory / readiness / prompt
→ 查询 allowed action 具体 capability
→ owning repo / workflow / canonical inputs / host / success criteria
→ 正式本机 Action
→ target run terminal
→ workspace artifact / manifest / receipt 实体验收
→ OpenWorker 原子写回
→ 再查询下一步
```

## 3. 固定机器权威

当前有效执行面是正式 GitHub Action + self-hosted Windows；机器真实性由 durable `assigned_host` 与真实 `COMPUTERNAME` fail-closed 校验。2026-08-17 曾出现的 assigned-host polling/spool service 已被显式 revert，不再作为 main 权威路径。

## 4. Worklist 与审批链

模板 revision=3，workspace=`D:\AI-Example\0002`，assigned_host=`DESKTOP-ODAQN0D`。

- 025：无图 OpenMAIC PPT；allowed=`presentation.openmaic`；验收 storyboard_pptx/manifest/SHA/slide_count/reopen_receipt/image_count。
- 027：用户审批无图 PPT；IMAGE 等待 027。
- 055：带图 PPT。
- 057：用户审批带图 PPT；VIDEO 等待 057。

Git 模板状态不是 ODA durable runtime 状态，不得用模板 PENDING 覆盖本机 evidence。

## 5. OpenMAIC durable evidence 与 OpenWorker adapter

OpenMAIC owning Action 应在 PPTX 同目录保存 `*.pptx + *.manifest.json + *.receipt.json`。receipt 包含 target run/attempt、artifact SHA/slide/media、manifest、runner computer_name。

`scripts/case0002_apply_openmaic_receipt.py` 重验 target run id、COMPUTERNAME、workspace containment、实体 SHA、manifest、OOXML ZIP、真实 `ppt/media/*`、slide_count；025 media=0，055 media>0。`accept_action_evidence(...)` 在同一 mutation lock 原子 PASS。

## 6. go-tool `/agent/query` 缺口与修复

原 `/agent/query` 没把 go-tool GitHub Action capabilities 放进 information pack，造成“模型问了 go-tool仍看不到工具”。现已增加 `action_capabilities.catalog/selected` 与 `<ActionCapabilityDirectory>` prompt；模型只能从注册 capability 选择，不确定时继续查具体 capability。

## 7. 0002 Operator evidence 顺序

```text
00-openworker-current.json
00a-runtime-health.json
01-agent-query.json
02-capability-detail.json
03-readiness.json
04-dispatch-request.json
05-dispatch-receipt.json
06-status-terminal.json
07-openworker-accepted.json
08-result.json
```

没有 `01-agent-query.json` 就不能证明“先查 go-tool 再做”。

## 8. 2026-08-18 本轮详实执行日志

### 08.1 10:18 — 用户新增记录铁律

用户要求继续案例，并要求每个步骤详实记录到案例文档。已将本节格式固定为后续执行日志模板。

### 08.2 10:18 — 读取 Git Worklist 模板

读取 `case-worklists/0002.json`：revision=3；workspace=`D:\AI-Example\0002`；assigned_host=`DESKTOP-ODAQN0D`。025 allowed action=`presentation.openmaic`，025 后必须进入 027；055 后必须进入 057。

注意：该文件是 Git 模板。当前 ODA durable `.openworker/case-worklist.json` 只能由本机 Action 读取，因此本轮尚不能用模板断言 durable canonical next step。

### 08.3 10:18–10:22 — 寻找可安全 rerun 的历史 Operator run

目的：当前 ChatGPT GitHub Connector没有新建 workflow_dispatch 的动作，但能 rerun 已知 run/job。因此只寻找与当前 `Operator E2E 0002 ComfyX-Studio` 同一 harness 的历史 run；禁止重跑旧 Studio source-to-film target 或 OpenMAIC CLI gate。

核对历史提交：

- `77c4a5c...`：0002 dispatch lock 改为 `GITHUB_RUN_ID + GITHUB_RUN_ATTEMPT`，说明 rerun attempt 是正式设计场景。
- `b5a7c193...`：concurrency `cancel-in-progress=true`，目标是只保留最新 formal dispatch harness。

Connector 限制：commit workflow-runs 只枚举 PR-triggered runs；通用 fetch 拒绝 Actions list-runs API；代码/文档搜索没有找到历史 harness run id。因此尚未取得可安全 rerun 的 job。

结论：`CONTROL-PLANE DISCOVERY BLOCKED`；产品步骤 025 尚未 start。

### 08.4 10:23 — 审核当前 Operator workflow main

读取 `.github/workflows/operator-e2e-0002-comfyx-studio.yml` 当前 main，确认：

1. 支持 `workflow_dispatch`，也支持对 harness/config 修改的 push；
2. permissions=`contents:write + actions:write`；
3. 3 个 generic Windows self-hosted slot；
4. 每个 slot 先 checkout，再用真实 `COMPUTERNAME` 路由；
5. 只有 `DESKTOP-ODAQN0D` 可取得 run-attempt lock 并执行；
6. ODA slot checkout OpenWorker main 作为 Worklist mutation authority；
7. 运行 `go run ./cmd/e2e-0002-dispatch`；
8. harness 失败时仅 block 自己拥有的 active execution；
9. always 发布 `evidence/latest-0002-dispatch.json`；
10. always 上传 runtime evidence（quota 允许时）。

当前仓库仍没有 `evidence/routes/latest-0002-route-slot-1.json`，也没有 `evidence/latest-0002-dispatch.json`。这证明本轮 main 尚无可见机器执行回执；不能误报 ODA 已接单。

### 08.5 10:24 — 审核当前 `cmd/e2e-0002-dispatch`

当前 harness 实际顺序已核对：

1. 建 `D:\AI-Example\0002\runs\runtime-dispatch-<run>`；
2. 目前仍有一条早期 `COMPUTERNAME == DESKTOP-ODAQN0D` 前置检查，随后又以 Worklist `assigned_host` 二次校验；这是可清理的重复硬编码，但不会改变当前 0002 机器结果；
3. 读取 durable `.openworker/case-worklist.json` 并写 `00-openworker-current.json`；
4. 读取 canonical next step / allowed_actions；
5. 启动本机 go-tool runtime，写 `00a-runtime-health.json`；
6. 构造基于当前 step/title/allowed/acceptance 的问题，POST `/agent/query`，写 `01-agent-query.json`；
7. allowed action 必须存在于 go-tool catalog，否则 fail closed；
8. GET capability detail，要求 `local_action`；
9. GET readiness，credential 必须 ready；
10. 用 OpenWorker helper `start` 建 active ownership；
11. canonicalInputs 只接受当前已实现 adapter；025 从 020.storyboard_request 取 request；055 从 050.bound_storyboard_request 取 request；
12. POST `/api/execution/dispatch`；
13. poll target 最长 23 分钟，必须 completed/success；
14. 找 persistent OpenMAIC receipt；
15. 调 OpenWorker receipt adapter，绑定 expected target run id；
16. 重新读取 durable Worklist；025 后 next 必须 027，055 后 next 必须 057；
17. 写 `08-result.json`。

本次只完成代码审计，没有机器 run，因此状态仍为 `IMPLEMENTED / NOT LIVE VERIFIED`。

### 08.6 本轮尚未产生的证据

截至 10:24：

- 无新 `01-agent-query.json`；
- 无 target run id；
- 无 storyboard PPTX；
- 无 manifest；
- 无 persistent receipt；
- 025 未 PASS。

## 9. 下一 REAL 验收点

必须继续沿相同路径取得：

```text
ODA durable Worklist current
→ /agent/query
→ registered allowed Action
→ capability detail/readiness
→ 0002-025 target Action
→ storyboard.pptx + manifest + receipt
→ physical QC media_count=0
→ atomic PASS 025
→ canonical next=0002-027
```

取得 PPT 后停止自动生产，交用户审阅。